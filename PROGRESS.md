# PROGRESS

One line per task. Status: todo / doing / review / done. Only the orchestrator sets `done`.
Completed phases are rolled up to one line each; the full done-check trails live in git history (`git log --format=%B`) and `REVIEW.md`.

## Rolled-up phases

| Phase | Tasks | Status | Summary |
|---|---|---|---|
| 0 Board | T00 | done | 2026-09-03: skeleton, coordination files, tasks T01–T14. `NORBIZ_SPEC.md` was never delivered → written by T01. `BRREG_MCP_FIRST_KRONE.md` never delivered → `HUMAN_TODO.md` §8 placeholder. |
| 1 Interface | T01 | done | `src/registry_mcp/` layout (D-003), pydantic models (D-004), `Registry` ABC + registration (D-008), spec with 97-test list. Found: build plan orgnr `833286602` invalid → typo for `833285602` (Kim confirmed); primary fixture `923609016` (Equinor ASA). |
| 2 Parallel impl | T02–T05 | done | Rules engine 100% coverage (one review cycle: D-009 fixed tax-return leak to public bodies/unknown forms). brreg client + cache, 3 live-verified fixtures, deleted entities return 200 not 410. CI/Docker/Caddy/deploy.md. llms.txt (39 lines), llms-full.txt, homepage JSON-LD, `server.json` valid against registry schema 2025-12-11. |
| 3 Surfaces | T06–T09 | done | REST (country-scoped routes, D-007 envelope, rate limit, static discovery routes). MCP (FastMCP 4.0.2, 5 tools, rules resource, prompt, Streamable HTTP at `/mcp` and `/mcp/` with no redirect, stdio `registry-mcp`). D-010 `DeadlineReport`/`ValidationResult` and D-012 `CountriesResponse` shared by both surfaces with parity tests. Logging (never raises) + `/v1/stats` + dashboard + UA classifier. |
| 4 Review, publish, launch | T10–T14 | done | 2026-09-04: architect checklist 8/10 → fixes; `uvx`/npm launchers verified; 4 articles × 3 versions; image + compose; clean-clone gate 273 passed. |
| 5 Second country (UK) | T15a–T15e | done | 2026-09-04, ~3 h spec→release, v0.2.0: `UK_SPEC.md`, D-015–D-021, `registries/gb/`, 16 live fixtures, Railway + PyPI + npm at 0.2.0. Carry-overs closed by later phases. |
| Research | R01–R03 | done | 2026-09-05: library at `~/research/registry-mcp/` (+ `08-united-states/`, `09-china-hong-kong-taiwan/`); distilled copy in `research/`; read `AGENT_PRIMER.md` first. |
| 6 Post-launch fixes | R01, T17–T25 | done | 2026-09-05: deadline citations (D-022/D-023), legibility (outputSchema, card, robots), evals harness, rules resources, content, D-024–D-031 roadmap rulings, ChatGPT aliases, playground, plugin marketplace + MCPB. |
| 7 Third country (Sweden) | T26r–T26h, T28–T34 | done | 2026-09-05→07: `SWEDEN_SPEC.md` + D-032–D-041; `registries/se/`; R-2 + F1 (D-040) landed; **0.3.0 released to PyPI/npm/MCP registry 2026-09-07**, Sweden live; T32 review's two blocking fixes applied (`07ada43`, `909f5b1`); T33 three live defects fixed (`e6b0097`); T26h launch content drafted (`6ece116`, Kim publishes). Open rows below. |

**New countries are on HOLD (Kim, 2026-09-06). Denmark access expected ~2026-09-28; decision was to wait, not chase.**

## Open rows carried from earlier phases

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| T16 | — | blocked | — | Denmark CVR: system-til-system access is granted by e-mail to cvrselvbetjening@erst.dk (Elasticsearch, up to 12 business days, then a declaration to sign about protected entities). Application **sent 2026-09-05** from Kim's Gmail; ERST acknowledged the same morning as **sagsnummer 177481**; answer expected by ~2026-09-23. Open questions in the mail: whether a Norwegian ENK without a Danish CVR number qualifies, and whether IP whitelisting is required (Railway has no fixed IP). | **2026-09-07 06:52Z: Erhvervsstyrelsen replied, 16 days early.** No answer to either question; the reply is a pointer to the self-service application at datacvr.virk.dk (`HUMAN_TODO.md` §7.6). Denmark is now Kim's online form, not a wait. IP-whitelisting question still open and it gates hosted use. **Application submitted 2026-09-07 13:48 via datacvr.virk.dk; brugeradgang promised in ~3 weeks (~2026-09-28).** IP-whitelisting question still unanswered and it gates hosted use — ask on the #177481 thread. **Kim 2026-09-07: wait it out — do not chase.** The IP-whitelisting question stays unanswered until access arrives (~09-28); nothing depends on Denmark meanwhile, so the cost of learning late equals the cost of learning now. Two constraints already on the record for whoever builds it: the route is a raw Elasticsearch cluster over **plain HTTP with Basic credentials in the clear** (HTTPS on that host times out), so the client must expect `http://` and those credentials must be used nowhere else; and if whitelisting *is* required, the hosted deployment cannot serve DK from Railway (no fixed address) — self-hosting works, a fixed-address proxy would fix hosted.
| T26d | Orchestrator | partly done | **2026-09-07: SWEDEN IS LIVE at 0.3.0** (commit 4fd1c92, deployed ~12:05Z). Pre-flight passed first (`count(*) where country='SE'` = 0 of 319). Railway now carries `BOLAGSVERKET_CLIENT_ID`/`_SECRET`/`_ENVIRONMENT=production`. `/health` → `0.3.0`, `[GB, NO, SE]`; `/v1/countries` shows SE/bolagsverket/`requires_api_key`. Live `GET /v1/SE/company/5560160680` → Telefonaktiebolaget LM Ericsson, active, AB, one clean industry code, source cited, 1 note; `/deadlines` → general_meeting 2027-06-30 and annual_accounts 2027-07-31, neither rolled. **D-040 verified in production: both SE rows logged `query = NULL`, while NO still logs `923609016`.** Version bumped in 10 manifests; CHANGELOG 0.3.0. **No git tag pushed, so npm/PyPI/MCP-registry are still at 0.2.0 — that release is a separate decision.** **Second pass (commit 0f952bc):** the "two countries" strings T26c deferred are now three — `api/main.py::_DESCRIPTION` (with Swedish keywords), `mcp/connector.py`'s `search` docstring — README lead rewritten, `SWEDEN_SPEC.md` linked, and a **README example generated from the running 0.3.0 service** (`5560160680` + both deadlines). Redeployed. Also fixed a CI flake unrelated to Sweden: GB test 105 bounded a mocked round trip at 0.5 s and failed at 1.17 s on a loaded runner (run 34120021824); it now asserts request overlap instead. | Still open: **nine fixtures carry `_VERIFY` and need the TEST credentials** (zip still not downloaded — both Bolagsverket mails share one Gmail thread and one filename, so three download attempts all fetched prod); **no git tag pushed, so npm/PyPI/MCP-registry stay at 0.2.0.** |
| T35 | Sonnet | doing | 2026-09-07: rebuild `static/index.html` as the chosen landing design — Direction B's structure and argument (leads with the failure; CARILLION PLC asked twice; the empty-deadline restraint proof) with Direction C's annotated payload plate in place of B's country table. Rulings: no web fonts (page stays self-contained), keep the existing token system and its dark block, playground behaviour untouched, nothing fires on load, NLOD attribution added and Sweden's licence quoted verbatim. Every fact re-verified against the live service. | — |
| T31a | — | todo → **folded into T44 + T46** | Part A of `tasks/T31.md`: the false sentence "Bolagsverket does not publish the financial year" is still served — `SWEDEN_SPEC.md:1140,1153,1776`, `registries/se/rules.py`, `mcp/server.py`/`api/main.py` deadline descriptions, `server-card.json`, `static/llms-full.txt` ×2, `README.md` (checked 2026-09-08 20:30Z). T26g/T26h landed, so nothing holds it; the `rules.py`/spec/surface-description half rides with T44 (same files), the card/llms-full/README half with T46. | T44 |
| T31b | — | **open** (block wired, rung unwired) | `include=["filings"]` block wired for SE in `e03a518` (D-044). **Not done:** `include` on `company_deadlines` (both surfaces), rung 2 in `se/rules.py` from `report.filings.financial_year_end`, N9's three states — `tasks/T31.md` Part B §4–§6 (`REVIEW.md` § D-044 wiring, finding 2). Becomes **T44**. | T41 writes T44 |

## Phase 8 — Depth per country (2026-09-08, Kim: "more registries on each folder would be MUCH better"; then "make the tasks and make it all live")

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| G1–G5 | 5 agents | done | 2026-09-07 night. **The finding that matters: demand is measurably tiny and the pitch was falsified.** Four comparable Norwegian register tools have 2/2/7/1 users; HN pays 2 points for this category; an enterprise MCP survey puts "other" at 2%. And BEC fraud impersonates a *real, active* supplier, so "check a supplier before you pay" does not survive contact with anyone who owns that risk. Directories are not distribution (5 listings → 5 calls, 2 of them a scanner); npm downloads are mirrors. **Discovery was working and delivering a stale Norway-only product** — 38 surfaces mapped, 8 listed, 5 wrong, including a LobeHub listing nobody knew existed calling this "pre-release 0.1.0". Fixed tonight: LobeHub issue #19266, mcp.so comment, Smithery description, GitHub About + 3 dead topics swapped for 3 reaching 958 repos, plugin marketplace entry, social cards, **legal pages served at all for the first time** (they 404'd since launch — an automatic rejection from the one channel with a push mechanism), and the container bug that made them 404 anyway. Dashboard gained by_operation, cache rate, latency and freshness. | — |
| R1–R4 | 4 agents | done | 2026-09-07 night → `~/mcp-growth/USE-CASE.md`, `COMPETITORS.md`, `DECISION-GATE.md` (day-45 criteria written before the data), `BASELINE-2026-09-07.md` + `evals` baseline arm (`38930e9`: the measurement does not say what we hoped). | — |
| D1–D2 | Opus ×2 | done | 2026-09-08: **D-042** (a second register call is an `include` attachment; R-5 promoted to first and is the gate; tool *count* → tool *rule*; officers/PSC stay barred) + `tasks/T37.md`; `~/mcp-growth/DEPTH.md` ranks every free source with fetched evidence. Committed `770c53c`. | — |
| R-5 | Sonnet | done | 2026-09-08: `SourceRef`, `Registry.supported_includes`, `Registry.lookup_with` (one assembler, bounded concurrency, failed attachment → absent block + `notes`), `include` on both surfaces (`10f46f2`); rendezvous concurrency test (`5bf0134`). Five of its claims proven load-bearing under mutation (S-series review). | — |
| T37 / R-5c | Sonnet | done | 2026-09-08: GB charges built behind the seam (`48cb6ab`) and shipped as `include=["charges"]` (`3ecc369`); `registers["charges"]` **removed** from GB reports — Companies House's `has_charges` is false for Tesco's nine charges (`beb287f`). GB filing history built (`9288390`), GB insolvency built minus practitioners (`411e50a`). | — |
| S-series | Opus reviewer | review received | 2026-09-08 (`REVIEW.md` § S-series, **uncommitted**): 31-point checklist; findings 1–3 applied (`beb287f`, `770c53c`); **4–10 open** → charge-shape ruling in T41, `charges.py` citations/counts in T40, `instructions` caveat + `_CallOutcome` + chart test in T45, provenance duplication in T40. | — |
| D-043 / T38 | Sonnet | done | 2026-09-08 22:55Z merged `067a1cc` (verified: footprint clean, `core/models.py` purely additive, 869 passed + the expected card drift, mypy/ruff clean). `include=["financials"]` NO; four models; **one in-flight `asyncio.Task` serves `filings` and `financials` on a cold cache — proven by request count through `lookup_with`** (D-043(h) rule 3); five new live fixtures; one T38 test expectation corrected against the wire (931883836 has no reconciliation gap) and reported. Brief was: 2026-09-08 (`fc4bb13`): Norway's key figures are a second block `include=["financials"]` — one fetch serving two blocks (h), currency required, no derived arithmetic; 573 live responses behind the entry. `tasks/T38.md` written; **not dispatched** — waits for T40 (same `no/accounts.py`). | T40 |
| R-5b/d/e wiring | Opus (orchestrator) | **review** | 2026-09-08 14:01 `e03a518`, **D-044**: `filings` wired in GB/SE/NO, `insolvency` in GB; canonical `FiledDocument`/`FilingHistory`/`InsolvencyBlock` in `core/models.py` (a measurement: all three modules had converged); four seam-pins replaced by a cross-country invariant. 834 passed. **Orchestrator review (`REVIEW.md` § D-044 wiring): APPROVED WITH FIXES** — 1 blocking (SE/GB scope note absent from `notes`, contradicting the published prose), 2 urgent (deadline rung untracked; this board), 5 non-blocking. **Not deployed** (served card predates it). | T39, T40 |
| T39 | Sonnet | done | 2026-09-08 20:45Z, merged `4693ac9` (verified by the orchestrator: 834 passed, mypy/ruff clean, diff = the six owned files, card test red under a one-character mutation of `_INCLUDE_DESCRIPTION`): card `inputSchema` regenerated + drift test extended; REST `include` text; `llms-full.txt`/README learn `filings` and `insolvency`; CHANGELOG `[Unreleased]` written from the commits. Findings 5, 6, 7. | — |
| T40 | Sonnet | done | 2026-09-08 22:05Z, merged `7143f9f` (verified: four done-check greps empty, diff inside footprint, 831 passed — three vacuous conversion tests out, two scope-note tests in — mypy/ruff clean; SE and GB blocks now lead with the scope note). **Found ten field-description facts D-044(a) claimed were kept and were not** → restored by T47 (addendum). Originally: scope note on every SE and GB `filings` block (finding 1, first commit); fold the four country stand-ins into `core.models` — one shape per concept, no `model_validate(model_dump())` (finding 4); `charges.py` citations and recon numbers (S-series 4, 10). | — |
| T41 | Opus A | done | 2026-09-08 21:05Z, merged `b2417ad`. **D-045** (eight parts): (a) charge field list ruled — `outstanding_count` struck, `part_satisfied_count` + `contains_fixed_charge` + `contains_negative_pledge` + two `*_type` tokens added, three flags are `True`-or-absent never `False`, `legal/terms.md` must name three text fields not one → **T47**; (b) **VIES refused** on its own terms (retransmission forbidden; a person's name in Ericsson's address on the first call); (c) FCA out this round, `include_api_key_env` mechanism ruled; (d) **Gazette refused** — its `robots.txt` disallows every machine-readable notice; (e) LEI on the base class via `universal_includes`, SE excluded (identifier in a third-party URL, D-039), per-kind TTL table by key segment → `tasks/T42.md`; (f) Peppol needs a recon task first (four facts named), no brief; (g) `Registry.deadline_report_with` + `DEADLINE_INCLUDES` → `tasks/T44.md` with T31a folded in; (h) fifteen sources ruled out in one sentence each. Live-verified GLEIF (RA000473 is Enhetsregisteret, RA000585 is E&W only, `registeredAs` wants the register's own spelling), VIES, the Gazette. Originally dispatched 20:15Z: **D-045** — charge shape ruled (S-series 4), VIES/FCA/Gazette in or out, LEI cross-country mechanism + per-kind TTL table, Peppol readiness, Swedish rung; writes `tasks/T42.md` (LEI), `T43.md` (Peppol, if ready), `T44.md` (Swedish rung + T31a), VIES brief if ruled in. | — |
| Deploy | Orchestrator | **done — LIVE** | 2026-09-08 20:47Z Railway deployment `3798e727` SUCCESS from `bf1f37f`. Smoke (orchestrator, prod): `/health` 0.3.0 `[GB,NO,SE]`; served card byte-equal to HEAD's; `/v1/countries` → GB `[charges, filings, insolvency]`, NO `[filings]`, SE `[filings]`; `?include=filings` → Equinor 1 doc FYE 2025-12-31, Tesco 25 of 8371 FYE 2026-02-28, **Ericsson `documents: []`** — block present, scope note first on all three; Tesco `insolvency` present `cases: []` with the no-resource note, `charges` 9/7 satisfied; `registers` carries `insolvency` only. **Open observation:** the largest Swedish company shows no filed annual report in Bolagsverket's document list — if the dataset holds digitally filed reports only, the SE scope note must say so (**checked:** `5561890038` Edition Reimers AB returns 6 reports on prod, newest 2025-12-31 filed 2026-06-30, so the dataset works and Ericsson is absent for a reason of its own — likely digital-submission-only; sourcing that and wording the SE notes is now in `tasks/T44.md`'s addendum). Original plan: `railway up --detach`, then `/health`, the served card == HEAD's, one `include=["filings"]` smoke on a documented example id per country. | T39, T40 |
| T42 LEI | Sonnet | done | 2026-09-08 22:40Z merged `e6e7ad1` (verified: 876 passed + expected card drift, footprint core-only, `grep lei registries/` empty, cache key `{C}:{reg}:lei:{id}` so the TTL table applies). `universal_includes` + `effective_includes`; SE excluded → `bad_request`; per-kind TTL table (`lei` 7 d / 24 h, env override ignored for declared kinds, five `not_found` stand-ins unchanged); GLEIF queries use `format_id` first (bare digits = 0 hits for NO/SE — the trap, tested); RA000472/585/587/544 asserted from responses; **missing-method guard now a `RuntimeError` before `lookup`** (S-series 6 closed). Loose end → T46: `gleif.aclose()` not wired into shutdown. Brief was: `tasks/T42.md` (D-045(e)). Core-only footprint; `include=["lei"]` in every country; GLEIF CC0 keyless; 7-day TTL = first user of the per-kind TTL table (`core/cache.py`), the pending item three modules fake with `status="not_found"`. | T41 |
| T45 | Sonnet | done | 2026-09-08 21:40Z merged `5e54a54` (verified: 835 passed, four new tests each proven red under reversal, diff = four owned files, card untouched and proven unaffected). Was: payment-fraud caveat in `instructions` (S5); `_CallOutcome.ok` on non-`RegistryError` exceptions (S6); dashboard chart test + `height='auto'` (S9). Footprint `mcp/server.py`, `api/dashboard.py`, tests. Round 2 with T38 and T42. | T39 |
| T44 | Sonnet | doing | 2026-09-08 23:00Z dispatched, worktree `t44`. `tasks/T44.md` (D-045(g)): T31 Part B §4–§6 (rung 2, `include` on `company_deadlines` both surfaces, N9 states) + T31a prose. Round 3, alone on the surfaces. | T41, round 2 |
| T47 | Sonnet | done | 2026-09-09 00:30Z merged `7ce3979` (verified: five owned files, five new fields with descriptions, `outstanding_count` gone, terms sentence names three fields, 924 passed + the expected card drift which the orchestrator regenerated at merge; mypy/ruff clean). Recount matched D-045(a) exactly (86/59/18, 19 of 19 `amount-secured`, `part_satisfied_count` 0 ×4); the brief's "Tesco carries a negative pledge" was wrong — all three flags occur only in NatWest's page, test written against that. Ten lost description facts restored from the pre-fold stand-ins at `411e50a`/`7d88faf`. Found: D-042(e)(3)'s `legal/terms.md` sentence had never been written; added as new text. Reconcile `Charge`/`ChargeBlock` with **D-045(a)** — production has served the unreconciled block since the 11:19 local deploy (served card carries `ChargeBlock`), no package release does; treat as a same-day correction with a CHANGELOG line. Footprint after T40: `core/models.py` (Charge/ChargeBlock only), `registries/gb/charges.py`, `legal/terms.md`, tests, four charge fixtures recounted. Round 3 with T44 (disjoint). | T40 |
| T48 Peppol recon | Opus | done | 2026-09-08 21:50Z merged `28889f9`: `tasks/T48-recon.md` (458 lines). **Buildable keyless** — SML DNS (U-NAPTR) → SMP HTTPS, both open; Equinor resolved live (17 document types, `smp.elma-smp.no`); Directory is sound for a positive, unsound for a negative (voluntary publication; 385,268 NO participants, 99.89% advertise BIS Billing 3.0 = EHF invoice; 2 queries/s). Needs a **four-point D-029 amendment** (`false` only via SML/SMP; `source` = the SMP that answered, one of 43 was not ELMA; a DNS resolver the project lacks — `dnspython` vs DoH is the architect's call; TTL is ours, upstream `no-store`) → **T49**. Licence: none published anywhere. Was: four facts before R-6 can be briefed — the SMP resolution route for a `0192:` participant, the Directory query endpoint and shape, Digdir/DFØ terms and limits, what `document_types` holds. D-043-shaped recon, one live resolution. Not urgent (1 Jan 2027). | — |
| T49 | Opus A | done | 2026-09-08 22:20Z merged `7856a2d`: **D-046** — `registered: false` only from the SML/SMP route, a Directory miss is `null`; `provenance.source` is the SMP host read from the NAPTR (a hardcoded host is a defect); resolver **`dnspython`** (already hash-pinned in `uv.lock` as a transitive), DoH refused — our chosen resolver in a statutory answer's trust path; TTL ours, kind `peppol`, key `NO:brreg:peppol:{orgnr}`, `null` never cached; `document_types` = ids only + derived `can_receive_invoice` by a two-id table, never `False` from the Directory; the Directory `contact` block never read; `license` carries a stated absence; `peppol` is a country declaration. `CORE_ROADMAP_SPEC.md` §8 updated; `HUMAN_TODO.md` §7.8 (ask Digdir about a licence, not blocking); **`tasks/T43.md` written — round 3 with T44/T47.** Two follow-ups taken: T46 §1 gains `peppol`; T42 review checks the `lei` cache key's third segment is `lei`. Verify `dnspython`'s licence before 0.4.0. Was: **D-046** amends D-029 per T48 (eight rulings incl. the resolver), updates `CORE_ROADMAP_SPEC.md` §8, writes `tasks/T43.md` (Peppol build, Norway-only). | — |
| T43 Peppol | Sonnet | doing | 2026-09-08 23:00Z dispatched, worktree `t43`. `tasks/T43.md` (D-029 as amended by D-046). Round 3 beside T44 and T47, disjoint footprints: `registries/no/peppol.py` new, `no/__init__.py`, `no/rules.py` (`rules_markdown()` gains the 2027 duty — `grep -c 2027` there is 0 today, so D-029(f) is unmet), `core/models.py` (`PeppolParticipant` before `CompanyReport`, field after `financials`), `core/cache.py` one row, `pyproject.toml` + `uv.lock` for `dnspython`. Trap: hardcoding `smp.elma-smp.no` passes 42 of 43 and is wrong. | T38, T42 |
| Deploy 2 | Orchestrator | **held** | Round 2 is merged and green on `main` but **not deployed**: `_INCLUDE_DESCRIPTION` still says "Three exist today" while five work, and shipping a false sentence in agent-facing prose is what finding 1 was about this morning. Deploys with T46 (which fixes the prose and bumps 0.4.0) after round 3. | T46 |
| T50 Anthropic track | Opus | done | 2026-09-09 00:05Z merged `fa9c685`: `content/anthropic-connectors-submission.md` (every portal field, two descriptions A/now and B/after-0.4.0 gated on `/health`, review criteria 20 PASS / 7 OPEN, data-handling answer with the four licences, reviewer test calls); plugin rebuilt as a job (`skills/company-check/SKILL.md` with the caveat verbatim, `/check-supplier`, `/filing-deadlines`, `/enrich-company-list`; `claude plugin validate` passes, incl. `--strict`); `HUMAN_TODO.md` §7.9; `SUBMISSIONS.md` §11. **Found: the served privacy policy is a labelled draft naming two countries — immediate-rejection material → T46 3c, before the next deploy.** Plugin submission lands in the *community* marketplace (argued from both docs pages). **Kim:** fix-then-deploy is ours; Team plan (~$50/mo) and the two submit clicks are yours. Dispatched 2026-09-08 23:00Z on "the anthropic track as well": Connectors Directory submission package (`content/anthropic-connectors-submission.md`, every portal field pre-filled, review criteria checked item by item, the data-handling answer with the four licences), `HUMAN_TODO.md` §7.9 (Team plan ~$50/mo is Kim's purchase; Kim submits), and the Claude Code plugin rebuilt as a job (SKILL.md + `/check-supplier`, `/filing-deadlines`, `/enrich-company-list`; `claude plugin validate`). | Kim: Team plan + submit |
| T46 | Sonnet | todo | Round-closing surfaces sync: `_INCLUDE_DESCRIPTION` + tool prose for `financials`/`lei`, card regeneration, llms-full/README/CHANGELOG, T31a's card/llms-full/README lines. Then Opus review of rounds 2–3, merge, deploy, **0.4.0 release** (PyPI before tag). | T38, T42, T44, T45 |
