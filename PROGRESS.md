# PROGRESS

One line per task. Status: todo / doing / review / done. Only the orchestrator sets `done`.
Format: `| id | owner | status | done-check result | blocker |`

## Phase 0 — Board setup

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| T00 | Orchestrator | done | 2026-09-03: skeleton, coordination files, tasks/T01–T14 written | `NORBIZ_SPEC.md` never delivered → T01 now includes writing it. `BRREG_MCP_FIRST_KRONE.md` missing → T14 leaves a placeholder |

## Phase 1 — Interface

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| T01 | Opus A | done | 2026-09-03: `uv sync --all-extras && uv run mypy . && uv run ruff check . && uv run pytest -q` → mypy clean (14 files), ruff clean, 12 tests pass. `list_countries()` → `['NO']`; `list_countries(include_stubs=True)` → `['NO','XX']`. `NORBIZ_SPEC.md` written (97 numbered tests); D-003…D-008 appended | Build plan's canonical org.nr `833286602` is **not valid** — fails MOD11 and 404s on the live API. Use `923609016` (Equinor ASA). Every done-check naming `833286602` needs correcting. Layout moved to `src/registry_mcp/` — see D-003 path note before assigning T02–T13 |

## Phase 2 — Parallel implementation

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| T02 | Sonnet 1 | done | 2026-09-03: B1/B2 fixed per `DECISIONS.md` D-009 — `deadlines_for` now returns `[]` when `legal_form_code` is missing or not a key of `ORG_FORMS` (`deadline_exemption_note` explains it), and `tax_return` is gated on the explicit `_TAX_RETURN_FORMS` set, no longer emitted for `ORGL`/unclassified codes. Also did the two non-blocking notes: `applies_because` now names the legal-form code, and `has_annual_accounts_duty=None` falls back to `legal_form_info(code)`. `uv run pytest tests/test_rules_common.py tests/test_rules_no.py --cov=registry_mcp.core.rules --cov=registry_mcp.registries.no.rules --cov-report=term-missing -q` → 89 tests pass, 100% coverage on both files (241/241 stmts). `uv run pytest -q -m "not live"` → 160 passed. `uv run mypy .` clean (27 files), `uv run ruff check .` clean repo-wide (incl. `api/`). `NORBIZ_SPEC.md` §5.4 `tax_return` row and §7 closing paragraph updated per D-009(d). | — |
| T03 | Sonnet 2 | done | 2026-09-03: `uv run python scripts/lookup_demo.py 923609016` (run 1) → live `CompanyReport` JSON, `cached: false`; run 2 → identical report, `cached: true`, same `fetched_at`. `uv run pytest tests/test_client_no.py tests/test_cache.py -q` → 32 passed (incl. 1 live-marked). `uv run mypy .` clean (23 files), `uv run ruff check .` clean repo-wide. No field names needed correcting — every §2 field verified against the live 923609016/974760673 fixtures (test 97). `registries/no/rules.py` (T02) landed mid-task; switched from mocked rules to the real `validate_orgnr` / `legal_form_info` / `derive_status` / `deadlines_for`-as-`deadlines` / `rules_markdown` before finishing. | **410-Gone VERIFY resolved, and it was wrong**: live deleted entities (found via `/oppdateringer/enheter?...&dato=...`, filtering `endringstype=="Sletting"`, e.g. `921084846`) answer **200** with a normal body carrying `slettedato` + `respons_klasse:"SlettetEnhet"`, never `410` — handled entirely by ordinary status derivation, no special-casing needed. `NORBIZ_SPEC.md` §1.1/§2/§6 updated with the finding; defensive `410→not_found` mapping kept in `client.py` for unconfirmed old/purged records. Pre-existing `tests/test_interface.py::test_norwegian_methods_raise_not_implemented` now fails (not my file, not touched) — same finding T02 already flagged: `validate_id`/`deadlines` no longer raise `not_implemented` now that both T02 and T03 are wired up; needs updating by whoever owns that file. **Opus A review 2026-09-03: APPROVED** — see `REVIEW.md` §T03. No field guessed (every §2 name re-checked against both committed fixtures), D-004…D-007 all hold, spec tests 82–97 all present. Non-blocking follow-ups only: three untested D-005 confidence anchors, untested cached-`not_found` / search-cache-hit / 429 paths, `test_97`'s tautological second half, raw `httpx.ConnectError` escaping `lookup`. |
| T04 | Sonnet 4 | done | 2026-09-03: `docker build -t registry-mcp:dev .` succeeds (legacy builder, no BuildKit/buildx available in this env — Dockerfile avoids `--mount=type=cache`); `docker compose config` valid. Runtime verification deferred to T13 (API module doesn't exist yet) | — |
| T05 | Opus B | done | 2026-09-03: `check-jsonschema --schemafile https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json server.json` → `ok -- validation done`. `wc -l static/llms.txt` → 39 (≤40). `static/index.html` JSON-LD `WebAPI` block parses (1 block, 16 keys). Also written: `static/llms-full.txt`, `KEYWORDS.md`, `HUMAN_TODO.md`, `NORBIZ_SPEC.md` §15 "Serving static files" | Placeholders `foretak` / `api.foretak.dev` throughout — see `HUMAN_TODO.md` §1. `README.md` still says `api.example.dev` (T11 owns it). `legal/terms.md` does not exist but the homepage JSON-LD `termsOfService` points at it |

## Phase 3 — Surfaces

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| T06 | Sonnet 3 | done | 2026-09-03: `uv run uvicorn registry_mcp.api.main:app --port 8080` boots; `curl /v1/NO/company/923609016` → full `CompanyReport`; `curl /v1/NO/validate/923609016` → `valid:true, formatted:"923 609 016"`; `curl /v1/SE/company/1` → 404 `unsupported_country` naming `/v1/countries`; `curl /openapi.json \| uv run --with openapi-spec-validator python -c "validate(...)"` → `openapi ok`; `uv run pytest tests/test_api.py -q` → 23 passed; `uv run mypy .` clean (27 files); `uv run ruff check .` clean repo-wide. Superseded by D-010: `/v1/{country}/validate/{id}` and `/v1/{country}/company/{id}/deadlines` now return `ValidationResult`/`DeadlineReport` via `Registry.validate`/`.deadline_report` (T07's session re-verified `uv run pytest -m "not live"` → 241 passed, `mypy`/`ruff` clean after that migration). | None blocking. Flagged for the architect in the T06 report: `Registry` ABC has no generic identifier-formatting hook, so `/v1/{country}/validate/{id}`'s `formatted` field uses a digits-in-3s heuristic in `api/main.py` rather than a real per-country formatter; and `deadlines_for` never attaches a calendar-year-assumption note to the response (only per-deadline `applies_because` text), so `api/main.py` synthesises one itself when any returned deadline is annual. Both are documented in code comments. |
| T07 | Sonnet 3 | done | 2026-09-04: `mcp/server.py` built on `fastmcp==4.0.2` (`FastMCP.http_app(path="/")` mounted at `/mcp` on `api/main.py`'s FastAPI app, its `.lifespan` composed into `_lifespan`). 5 tools (`lookup_company`, `search_company`, `company_deadlines`, `validate_company_id`, `list_countries`), resource `registry://rules/{country}`, prompt `explain_company` — all dispatching via `core.registry.get_registry`, no Norwegian import. Every tool/`RegistryError` becomes a `fastmcp.exceptions.ToolError`/`ResourceError` whose text is `json.dumps(err.to_dict())`, verified round-tripping through a real `fastmcp.Client` call. stdio: `src/registry_mcp/__main__.py` + `[project.scripts] registry-mcp = "registry_mcp.mcp.server:main"` (`uv sync --all-extras` after). Done-check: `uvicorn` + `curl /health` ok; `claude mcp add --transport http` → `claude mcp list` shows `registry-mcp ... ✔ Connected` → removed; stdio `initialize` and `tools/list` over `uv run registry-mcp` return clean JSON (5 tools) on stdout, no banner noise; `uv run pytest -m "not live"` → 241 passed (13 new in `tests/test_mcp.py`, incl. the D-004 REST/MCP-identical-JSON test); `uv run mypy .` clean (39 files); `uv run ruff check .` clean. | None blocking. Awkward spots for the architect: (1) `Registry`/`core/` has no shared date-parsing helper, so `mcp/server.py::_parse_today` duplicates `api/main.py::get_deadlines`'s `today` validation verbatim — a `core.rules.common` or `Registry` hook for this would remove one of the two copies. (2) A resource error crosses the wire as a plain JSON-RPC error, so the client-side exception type is `mcp.shared.exceptions.MCPError`, not `fastmcp.exceptions.ResourceError` (that one is only ever seen server-side) — `str(exc)` still carries the same `{"error": {...}}` text, so D-007 parity holds, but a future reader of `tests/test_mcp.py` should not assume the exception class matches the one raised in `mcp/server.py`. (3) `list_countries`'s `{"countries": [...]}` payload has no `core/models.py` type (by design — `api/main.py`'s own `CountriesResponse` is REST-only too) so both surfaces independently re-derive it from `Registry.describe()`; harmless today but is the one payload D-004 doesn't cover. |
| T08 | Sonnet 3 | done | 2026-09-04: wiring step on top of the already-committed `core/log.py`/`core/stats.py`/`api/stats.py`/`api/dashboard.py`. `api/main.py`: `record_call = log.log_call`; `app.include_router(stats_router)` + `app.include_router(dashboard_router)` (both `include_in_schema=False`, admin/debug routes, not the versioned data API); static routes and `/health` confirmed never call `_record`. `mcp/server.py`: `record_call = log.log_call`; one shared `_call_context` contextmanager (timing + `RegistryError`→`ToolError` + `record_call` in a `finally`) used by all 5 tools instead of five try/except copies; user agent via `fastmcp.server.dependencies.get_http_headers()` (`"user-agent"` header on Streamable HTTP, `"stdio"` fallback — verified live: real HTTP MCP calls logged with the client's real UA, e.g. `python-httpx2/2.12.0`). Also folded T07's `_parse_today`/`api/main.py`'s inline duplicate into `core/rules/common.py::parse_iso_date(value, *, field="today")` (new, small, country-neutral `core/` edit — flagged for the architect at T10, see note). New `tests/test_wiring.py` (3 tests) exercises the real `app` + real in-process `fastmcp.Client` together via `log.set_sink()`; 4 new `parse_iso_date` tests appended to `tests/test_rules_common.py`. Live done-check: `uvicorn` + `REGISTRY_MCP_ADMIN_KEY=test123`, 6 REST calls (2 user agents) + 4 MCP calls via `fastmcp.Client("http://localhost:8080/mcp")` → `curl "localhost:8080/v1/stats?key=test123"` → `total_calls: 10`, `by_surface: {"rest": 6, "mcp": 4}`, `distinct_user_agents: 3`; `curl localhost:8080/v1/stats` (no key) → 403; `curl .../v1/stats/dashboard?key=test123` → 200 HTML. `uv run pytest -m "not live"` → 248 passed; `uv run mypy .` clean (40 files); `uv run ruff check .` clean. | None blocking. `core/rules/common.py::parse_iso_date` is a new shared helper the architect should look at for T10: it collapses what were two independent copies of the same `today`-parsing/`bad_request` logic (`api/main.py::get_deadlines`, `mcp/server.py::company_deadlines`) into one, at the cost of that error no longer carrying a `country` field (the exact signature given for this step, `parse_iso_date(value, *, field="today")`, has no `country` parameter) — a minor, deliberate regression from the two prior copies, unobserved by any existing test. |
| T09 | Sonnet 4 | done | — | router include in api/main.py deferred to wiring step |
| T10 | Opus A | doing | — | Blocked on T06–T09. Carry-over for review: brreg omits `antallAnsatte` when `harRegistrertAntallAnsatte` is true (833285602) → mapper yields employees=None; decide 0 vs None |

## Phase 4 — Publish and launch

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| T11 | Opus B | doing | — | running in parallel with T10 review |
| T12 | Opus B | review | — | 12 article files + `content/README.md`, CSVs and `call.py`; every block real server output; schedule appended to `HUMAN_TODO.md` §6 |
| T13 | Sonnet 4 | review | 2026-09-04: Fixed the T04-flagged Dockerfile gap — runtime stage now also `COPY`s `/app/static` and `/app/server.json` from the builder and sets `REGISTRY_MCP_STATIC_DIR=/app/static`. `docker compose up --build -d` failed on this host's rootless Docker (`cannot expose privileged port 80`) even with nothing else listening on 80/443, so added `docker-compose.local.yml` (`-f` override, remaps Caddy to 8080/8443, `ports: !override` to replace rather than merge). Full stack up with `REGISTRY_MCP_DOMAIN=localhost` (Caddy internal CA): `curl -k https://localhost:8443/health` → `200 {"status":"ok","version":"0.1.0","countries":["NO"]}`; `.../v1/NO/company/923609016` → `200` full `CompanyReport` (EQUINOR ASA); `.../llms.txt` → `200`; `.../server.json` → `200`; `.../` → `200` HTML; `.../status` → `200` HTML (version `0.1.0`, countries `NO`, upstream `reachable`, cache rows `1`) — confirming the Dockerfile fix (all four static/discovery routes 404'd before it on a fresh image build). MCP: `POST /mcp` (no trailing slash) is a `307` (Starlette `Mount` redirect, confirmed independent of Caddy/TLS by hitting the `api` container directly over plain HTTP) that `fastmcp`'s `StreamableHttpTransport` doesn't follow for POST; `https://localhost:8443/mcp/` (trailing slash, `verify=False`) works — `client.list_tools()` → 5 tools; `claude mcp add --transport http registry-mcp-docker https://localhost:8443/mcp/` → `claude mcp list` → `✔ Connected`, then removed. Persistence: looked up `923609016` (`cached: true`, `fetched_at: 2026-09-04T06:21:54.670406Z`), `docker compose restart`, looked up again → `cached: true`, **same** `fetched_at` — SQLite cache confirmed persisted on the `registry-data` named volume across a full container restart. New `api/status.py` (`GET /status`, `include_in_schema=False`, no secrets/per-query data, upstream brreg check cached 60 s) wired into `api/main.py` via the one import + one `include_router` line; `tests/test_status.py` (8 tests, upstream mocked via `respx`) all pass. `uv run pytest -q -m "not live"` → all pass (one pre-existing flaky failure, `test_rate_limit_429_shape`, reproduced identically with my changes stashed out — not caused by or in scope for T13). `uv run mypy .` clean (45 files). `uv run ruff check .` clean on every file T13 touched (one pre-existing unrelated `content/03-enrich-spreadsheet/enrich.py` SIM115 from a concurrent agent, not touched here). `deploy.md` updated: removed the never-working "curl the api container's published port directly" line, added "Testing this locally" + "Smoke test" + "Corrections found while verifying this doc" sections. `docker compose -f docker-compose.yml -f docker-compose.local.yml down -v` at the end; `docker ps -a` / `docker volume ls` confirm no `registry-mcp-*` containers or volumes remain. | Flagged for whoever next touches `api/main.py`'s MCP mount: `mcp.http_app(path="/")` mounted at `/mcp` 307-redirects a trailing-slash-less `POST /mcp` to `/mcp/`, which not every MCP client follows for POST — a one-line fix (`redirect_slashes=False` on the mount, or mounting at `/mcp/`) would remove the trap; not fixed here since it's outside T13's one-line `api/main.py` allowance. |
| T14 | Orchestrator | todo | — | Blocked on T11–T13 |
