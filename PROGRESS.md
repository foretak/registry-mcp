# PROGRESS

One line per task. Status: todo / doing / review / done. Only the orchestrator sets `done`.
Completed phases are rolled up to one line each; the full done-check trails live in git history (`git log --format=%B`) and `REVIEW.md`.

## Rolled-up phases

| Phase | Tasks | Status | Summary |
|---|---|---|---|
| 0 Board | T00 | done | 2026-09-03: skeleton, coordination files, tasks T01–T14. `NORBIZ_SPEC.md` was never delivered → written by T01. `BRREG_MCP_FIRST_KRONE.md` never delivered → `HUMAN_TODO.md` §8 placeholder. |
| 1 Interface | T01 | done | `src/registry_mcp/` layout (D-003), pydantic models (D-004), `Registry` ABC + registration (D-008), spec with 97-test list. Found: build plan orgnr `833286602` invalid → typo for `833285602` (Kim confirmed); primary fixture `923609016` (Equinor ASA). |
| 2 Parallel impl | T02–T05 | done | Rules engine 100% coverage (one review cycle: D-009 fixed tax-return leak to public bodies/unknown forms). brreg client + cache, 3 live-verified fixtures, deleted entities return 200 not 410. CI/Docker/Caddy/deploy.md. llms.txt (39 lines), llms-full.txt, homepage JSON-LD, `server.json` valid against registry schema 2025-12-11. |
| 3 Surfaces | T06–T09 | done | REST (country-scoped routes, D-007 envelope, rate limit, static discovery routes). MCP (FastMCP 4.0.2, 5 tools, rules resource, prompt, Streamable HTTP at `/mcp` and `/mcp/` with no redirect, stdio `registry-mcp`). D-010 `DeadlineReport`/`ValidationResult` and D-012 `CountriesResponse` shared by both surfaces with parity tests. Logging (never raises) + `/v1/stats` + dashboard + UA classifier. |

## Phase 4 — Review, publish, launch

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| T10 | Opus A | done | 2026-09-04: 10-point checklist 8 PASS / 2 FAIL → B2 (`aclose`) and B3 (lifespan `finally`) fixed at 79b18bd / 7eba739; B1/B4 (flaky 429 test, `/mcp` 307) fixed at 7eba739; N1–N8 all applied. New decisions D-011 (employees invariant), D-012 (countries model), D-013 (validate reason), D-014 (`Registry.aclose`). | — (sign-off APPROVED, see REVIEW.md) |
| T11 | Opus B | done | `uvx --from dist/*.whl registry-mcp` → 5 tools; npm launcher → 5 tools (needs the PyPI package to exist — fails correctly pre-publish); alias `brreg-mcp` built; README/CONTRIBUTING/issue templates/seed issues/`legal/terms.md`/`glama.json`/Smithery card/`SUBMISSIONS.md`. | **npm `brreg-mcp` is taken** (hellosverre, 2026-04) → alias scoped `@foretak/brreg-mcp`; human decision in `HUMAN_TODO.md` §3 |
| T12 | Opus B | done | 4 articles × 3 versions, every JSON block real (`content/call.py`), devto ≤600 words (435/480/480/566). VAT article uses `833285602`. | — |
| T13 | Sonnet 4 | done | Image ships `static/` + `server.json`; compose up via Caddy on 8443 (rootless Docker can't bind 80/443 → `docker-compose.local.yml`); `/health`, company, llms.txt, server.json, `/`, `/status` all 200 over HTTPS; MCP 5 tools over HTTPS; cache survives restart (identical `fetched_at`). Found the `/mcp` 307 bug → fixed in T07's files. | — |
| T14 | Orchestrator | done | 2026-09-04 clean clone (`git clone . scratchpad/clean`): 273 passed, 1 deselected (live); mypy clean (45 files); ruff clean; wheel stdio → 5 tools; `docker build` ok; `docker compose config` ok with `.env.example`; secret grep empty. `HUMAN_TODO.md` §8 (FIRST_KRONE placeholder) + §9 (launch sequence) written. | **Feature work frozen.** Next agent task is not code: read the stats dashboard for two weeks (build plan §4.2). Human items: `HUMAN_TODO.md` §9 |

## Carry-overs for T15 (second country)
- Seven tests hard-code the country list and `test_unsupported_country` uses `SE` — the test suite, not `core/`, is what country #2 must edit (T10 note).
- `content/04-add-your-country` names Denmark (CVR) / Sweden (Bolagsverket) as first targets.
- npm launcher has no offline/dev fallback; it always resolves `registry-mcp` from PyPI.

## Phase 5 — Second country (started 2026-09-04 17:15, **shipped 20:25** — ~3 h spec→release vs ≤2-week target; core needed one country-neutral addition, D-018)

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| T15a | Opus A | done | 2026-09-04: `UK_SPEC.md` (16 sections, 109-test list); D-015 (`GB` strict, no `UK` alias), D-016 (UK deadline policy — CH-published dates authoritative, compute only from a sourced rule, no roll-forward, corporation tax documented not computed), D-017 (`Registry.requires_api_key`/`api_key_env` surfaced by `country_info()`, defaults leave NO/XX untouched). Key arrived mid-task → **16 live payloads saved to `tests/fixtures/ch_*.json`**, resolving all but 5 `VERIFY` markers and finding 8 places the published schema is wrong (§1.6). `uv run pytest -q -m "not live"` 273 passed / 1 deselected; mypy clean; ruff clean. | — |
| T15b | Sonnet | done | 2026-09-04 (resubmission after T15e BLOCKED on B1): **B1 fixed** — `rules.deadlines_for` is now the pure `(report, today)` the ABC always required; `mapping.map_entity` fills `report.published_deadlines` (D-018) from the raw payload at lookup time, `_accounts_deadline`/`_confirmation_deadline` merge published-vs-computed off `PublishedDeadline` objects, `deadline_exemption_note` reuses `deadlines_for` at the probe date instead of re-deriving from raw data. `client.py::raw_for` deleted; the entity cache still stores raw JSON (mapping-fix-friendly) but is never read back as a transport. New regression tests: `test_b1_deadline_report_survives_cache_disabled` and `test_b1_deadlines_for_is_pure_without_any_cache` (monkeypatches `core.cache.get`/`set` to raise — proves no I/O at all). Tests 51–72 rewritten to `mapping.map_entity(data)` → `deadlines_for(report, today)`, exercising the real path. **D-019**: `registries/no/client.py`'s 429 → `rate_limited` (was `upstream_error`), `NORBIZ_SPEC.md:278` updated, new respx test. **4 nits**: token bucket now acquires inside the retry loop (N-1); `map_previous_names` uses `.get("name")` and drops empties (N-2); search `total` defaults to `0` per spec (N-3); test 72 adds a source-inspection assertion that no file under `registries/gb/` mentions `roll_forward` (N-4). **Mid-round product fixes** (coordinator-relayed, matching T15c's own 3 findings independently): `legal_form_local` is now always `None` for GB (not a copy of `legal_form`); GB `not_found` no longer forwards Companies House's `request_id` into `RegistryError.details` (`details == {}`); **D-020** (`SearchResult.hits` confidence-descending, architect's `core/models.py` validator) — added the non-increasing-confidence assertion to GB test 90 and NO's `test_search_maps_hal_envelope`, no registry code needed; **D-021** (`Registry.id_caveat` hook, architect's `core/registry.py` addition) — implemented `CompaniesHouseRegistry.id_caveat` from the §5.1.2 prefix table (as-of `2026-09`), `"ZZ12"` validates `true` with the caveat in `reason` and `hint is None`, `"SC090312"` validates `true` with no caveat. Runs: `uv run pytest -q -m "not live"` **404 passed / 5 deselected** (up from 391); mypy strict clean (52 files); ruff clean; 4 `@pytest.mark.live` GB tests green with the real key; orchestrator verifier 42/45 — **FRAGILITY CHECK now PASSES**, the other two named failures are the same pre-existing verifier bugs, the third (`api/main.py` "GB" grep hits) is Opus B's concurrent doc-string edits, not GB registry logic. | Awaiting T15e re-review |
| T15c | Opus B | done | 2026-09-04: docs, code strings, version 0.2.0, article 5. **Every example regenerated from a live server** (port 8095, real Companies House key, re-verified after D-018 landed). Docs: `static/llms-full.txt` (GB section mirroring NO — countries table, `/v1/countries` with `requires_api_key`/`api_key_env`, GB lookup/search/deadlines/validate examples, `published_deadlines` documented per D-018, GB error causes incl. the missing-key `upstream_error` and CH's 600/5-min limit), `static/llms.txt` (**40 lines**, GB in the lede), `static/index.html` (GB curl block beside NO, meta/JSON-LD keywords + `areaServed` GB), `README.md` (UK lookup + deadlines blocks, "Countries: Norway (brreg), United Kingdom (Companies House)", `COMPANIES_HOUSE_API_KEY` row), `CONTRIBUTING.md` (GB as the receipt for "one folder", links `registries/gb/`, D-017 named as the one honest `core/` gain), `KEYWORDS.md` §GB (7 rows + two placement-table rows). `server.json`: description 91 chars, 20 keywords, `countries: ["GB","NO"]`, `COMPANIES_HOUSE_API_KEY` on both packages, 3× version — **schema-validated green**. Code strings only (no logic): `mcp/server.py` `instructions` + 5 tool docstrings, `api/main.py` `_DESCRIPTION` + `_COUNTRIES_EXAMPLE` (D-017 follow-ups closed). Version 0.2.0 in `pyproject.toml`, `__init__.py`, `packages/brreg-mcp/pyproject.toml` (+ `registry-mcp==0.2.0` pin), both npm `package.json`, `server.json` ×3, `uv lock` re-resolved. Article 5 `content/05-uk-companies-house/{devto,reddit,no}.md` (599 / 188 / 594 words), Tesco `00445790` + Deloitte LLP `OC303675`, added to `content/README.md` (day 11) and `HUMAN_TODO.md` §6. Checks: 393 passed / 5 deselected, mypy clean (52 files), ruff clean. `ruff format --check` fails on 23 files — **pre-existing and repo-wide** (incl. `core/models.py`, 6 tests, 3 specs); none of the diff hunks are in text I wrote. **Product feedback for T15e**, 3 items: (1) GB search hits are returned in the register's relevance order, so a `0.4` hit can sit above a `0.8` one — README's "best first" was wrong and is now fixed in prose, but sorting or documenting it in `core` is the real call; (2) `validate_company_id("ZZ12", country="GB")` returns `valid: true, normalized: "ZZ000012"` — any two letters pass, so a spreadsheet column of bad prefixes gets a green light the register can't honour; (3) `legal_form_local` duplicates `legal_form` verbatim for GB, so a UI rendering both shows "Public limited company (Public limited company)" — `UK_SPEC.md` §2 asked T15c to rule: recommend `None`. Also confirmed T15e's B1 independently before D-018 landed (GB deadlines returned `[]` **with empty `notes`** under `REGISTRY_MCP_CACHE_DISABLED=1`); re-verified fixed. | Ready for review; T15d unblocked |
| T15d | Orchestrator | done | 2026-09-04: Railway on 0.2.0 (`/health` → GB,NO), GB smoke live; PyPI 0.2.0 ×2; npm 0.2.0 ×2 (passkey web-auth; OIDC workflow prepared, trigger off until trusted publisher registered — HUMAN_TODO §7.5); tag v0.2.0 → MCP registry 0.2.0 isLatest; Smithery re-scanned; dev.to article 5 drafted (4576602); `uvx`/`npx` from clean cache list GB+NO. | — |
| T15e | Opus A | done | 2026-09-04: 10-point GB checklist **9 PASS / 1 FAIL** — see `REVIEW.md` §T15e. PASS: zero `core/`/`api/`/`mcp/` edits (country #2 really was one folder + one import line); all 8 §1.6 traps handled (`has_charges` not `links.charges`, `open`→ACTIVE, 11-key stubs→UNKNOWN, no ARD parsing, `"null"` string never read); D-015 identifier rules exact (prefix table not a gate, never truncates, `UK` rejected); D-016 ladders exact and TESCO's due dates equal CH's own `next_due`; client (Basic auth, 600/5-min bucket, 429→`rate_limited`, 401→`upstream_error` naming the env var, key read at call time, D-006, `aclose`); tests 1–109 one-per-number with 106–109 live and green; REST≡MCP parity for GB + `requires_api_key` on `/v1/countries`. Runs: 391 passed / 5 deselected, mypy clean (52 files), ruff clean, 4 live GB tests green (incl. test 109 re-fetching all 12 fixtures). Orchestrator verifier 42/45 — two of its three failures are verifier bugs (`report.has_charges` is `registers["charges"]`; `"SC12-34"` correctly normalises to `SC001234` because §5.1 strips `-` first), the third is real. **FAIL (B1, BLOCKING)**: `deadlines()` reads the raw payload back out of the SQLite cache (`client.raw_for`), violating the ABC's "pure, no I/O" contract — with `REGISTRY_MCP_CACHE_DISABLED=1` an active TESCO PLC yields **zero deadlines and zero notes**, silently. Ruled as **D-018**: `core/models.py` gains `PublishedDeadline` + `CompanyReport.published_deadlines` (architect's edit, made; NO/XX untouched, suite green) so the register's own dates travel on the report and `deadlines()` stays pure. **D-019**: an upstream 429 is `rate_limited` everywhere — Norway aligns to Britain (3-line fix, no test pins it). Fix list with file:line in `REVIEW.md` §T15e, owner T15b. Time: UK spec ~17:15 → module finished ~19:00 the same day, against a ≤2-week target. | Verdict **BLOCKED on B1 alone**; T15c/T15d stay blocked until it lands |
| T16 | — | todo | — | Denmark CVR: system-til-system access is granted by e-mail to cvrselvbetjening@erst.dk (Elasticsearch, up to 12 business days, then a declaration to sign about protected entities). Application **sent 2026-09-05** from Kim's Gmail; ERST acknowledged the same morning as **sagsnummer 177481**; answer expected by ~2026-09-23. Open questions in the mail: whether a Norwegian ENK without a Danish CVR number qualifies, and whether IP whitelisting is required (Railway has no fixed IP). |

## Research (not code) — 2026-09-05

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| R01 | Orchestrator + 7 Opus researchers | done | 2026-09-05: research library at `~/research/registry-mcp/` (7 folders, ~154k words, every fact sourced per `BRIEF.md`); distilled layer committed in `research/` (`AGENT_PRIMER.md` + 7 summaries). Immediate actions taken: Smithery description set (was empty, unsearchable); NAMES.md addendum (Proff Premium MCP, Apier, pipeworx-io, bouch, ENTIA). Flags for Kim: (a) possible citation errors in NO deadline rules (`research/03-regulation-drivers.md` §"What this means", file 12) — needs an Opus A review before any fix; (b) backlog items 1–5 in `research/07-product-improvements.md` are legibility fixes touching no `core/` (schemas, annotations, README, CHANGELOG/SECURITY) — candidate exceptions to the freeze; (c) build plan §5.3 revenue target is 12–30× Proff's seat price — re-examine at the gate. | — |

## Phase 6 — Post-launch fixes authorised by Kim 2026-09-05 (exceptions to the freeze: no `core/` change, no response-shape change)

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| R01 | Opus A | done | 2026-09-05: review of the Norwegian deadline citations raised by the research library (`research/03-regulation-drivers.md`). Verdicts: roll-forward for `annual_accounts`/`general_meeting` had **no legal basis** and made the answer later than lawful (regnskapsloven § 8-3(1) fee runs unless sent *before* 1 August) — CONFIRMED; MVA term 3 = 31 August — already correct; calendar-year note — honest but misplaced and understated. D-022 (cite only the provision that contains the rule; roll-forward decided per deadline from its own source) and D-023 (calendar-year caveat wording/placement; Regnskapsregisteret route deferred). Fix list F1–F10 in `REVIEW.md` §R01. | — |
| T18 | Sonnet | done | 2026-09-05: F1–F4, F7, F8, F9 (content + llms-full), F10 implemented; `content/call.py` fixed (`.data` → `.structured_content` after real output schemas). Equinor `annual_accounts` for today=2026-10-01: `2027-08-02 rolled_forward=true` → `2027-07-31 rolled_forward=false`. dev.to article 4575628 body updated 2026-09-05 13:21Z with the regenerated blocks. 417 passed / mypy / ruff clean. | — |
| T17 | Sonnet | done | 2026-09-05: legibility backlog 1–6 from `research/07-product-improvements.md`: real `outputSchema` from the pydantic models (FastMCP dereferences `$ref`s on the wire; wire bytes unchanged, parity tests green), `ToolAnnotations` + titles on all five tools (read-only, idempotent, `openWorldHint` false for validate/list_countries), parameter descriptions + examples (no `pattern` bounds — they would pre-empt the JSON error envelope and break REST≡MCP parity), README first screen (deadlines/freshness-vs-30-days/five tools + Security + VS Code/Cursor badges), `CHANGELOG.md`, `SECURITY.md`, `X-Request-ID` middleware, `GET /.well-known/mcp/server-card.json` (regenerated, 0.2.0) and `GET /robots.txt`. Tests 404 → 417. | — |
| T19 | Sonnet | done | 2026-09-05: agent eval harness (`evals/`), golden mode (no LLM, offline cases in CI) + agent mode (Anthropic SDK, opt-in, `eval` dependency group). Backlog item 6. Delivered: 26 cases (E01–E26), golden 23 pass / 3 skip (2 live-only, 1 agent-only), CI step added, `anthropic` in an `eval` dependency group only; agent mode validated against a scripted fake client (no API key in the sandbox). **Run 2 (harness v2, commit below): 23 pass / 1 fail / 2 skip on the real model.** Fixes: generic fixture mocks + `GAP` status, structured checks on captured tool results, date/wording variants, negation-aware fabrication gate, `today` wildcards. Genuine finding kept as a hard gate: E19 — the model answers "is Tesco PLC VAT registered" from general knowledge without calling `lookup_company` (candidate fix: tool description that says the tool states explicitly when a register does not publish VAT). | — |
| T20 | Sonnet | done | 2026-09-05: concrete `registry://rules/NO`/`GB` resources (item 9); honest-caveat trio — ENK personal-data note, `parent_id` group-walk doc, "does not screen sanctions/PEP/bank accounts" (item 10); 400×400 icon served at `/icon.png` (unblocks Cline). | — |
| T21 | Opus B | done | 2026-09-05: research folded into content. `content/01-vat-check/` all three versions carry NS 116/2018 (kr 691,100 + kr 138,220), NS 27/2019 (kr 492,541 + 2×20%), bokføringsforskriften § 5-1-2 and Rundskriv 15/2019 § 4.4.1 (three-month oppslag / one-month customer-supplied / notoritet), blocks regenerated live; dev.to 4575622 PUT 14:49:49Z, still published. `llms-full.txt` §9 + a README section under Tools (Rundskriv, 1 Jan 2027 e-invoicing + ELMA, AMLR Art. 23(4) from 10 Jul 2027, the five provenance fields, the limits). `content/reddit-r-claudeai-post.md` (248 w) and `content/reddit-uk-developers-post.md` (250 w). NAMES.md positioning paragraph. Correction: the one-month rule is scoped to customer-supplied firmaopplysninger, three months for a register oppslag — `research/06` §6 and `AGENT_PRIMER` §2.8 read it too broadly. | — |
| T22 | Opus A | done | 2026-09-05: decisions D-024…D-030 (batch lookup, NACE, euid/LEI/advertising-protection, sub-national key, officers/PSC preconditions, ELMA/Peppol capability, hosted metering) + `CORE_ROADMAP_SPEC.md`. Rulings: batch = argument `company_id: str | list[str]` (cap 50, per-row D-007 errors, `POST /v1/{cc}/companies`); NACE via committed `core/nace.py`, `nace_class()` returns None on unknown; `euid` verbatim, `advertising_protected: bool | None` default None + paired note; LEI/Peppol/officers as `include=[...]` attachments with their own `SourceRef` (never fields on the register document); sub-national via `subdivision` (ISO 3166-2) + `register_office`, D-015 unamended; officers need four preconditions (company→officers only, 1 h person TTL, `suppressed: None` until CH signalling observed, lawyer-reviewed basis); Peppol = second upstream, 24 h positive / 1 h negative TTL; metering = Bearer key, stdio + anonymous `/mcp` keyless **forever**, one unit per identifier incl. cache hits, validate/list_countries never metered. Order R-1 batch → R-2 euid+advertising → R-3 sub-national → R-4 metering → R-5 attachments+LEI → R-6 Peppol → R-7 NACE → R-8 officers; all touch `core/`, none before T19's eval exists. Open for Kim: the five-tool count was ratified inside D-024 (it was never a numbered decision); D-024's `anyOf` outputSchema needs a FastMCP check or falls back to a sixth tool. 422 passed. | — |
| T23a | Opus A | done | 2026-09-05: **D-031 ruled — we ship `search` and `fetch`; five tools become seven, stated as "five registry tools plus two connector aliases".** OpenAI's contract read at `developers.openai.com/api/docs/mcp` (the 301 target of `platform.openai.com/docs/mcp`) and quoted in the entry. Rulings: aliases over existing operations, no new `core/` model and no REST twin; `id` = `"{COUNTRY}:{identifier}"`; `url` = our own REST record URL (always non-empty — OpenAI drops the citation on a blank one — and it resolves to the same bytes `fetch` returned); `search(query)` derives country from the query and otherwise **fans out** across every live registry rather than defaulting to NO (a default is a guess presented as an answer); an identifier short-circuits to a lookup via `Registry.validate`; zero hits returns real `rules:{CC}` documents, never a fabricated row; `fetch` renders Markdown in `text` **including deadlines** (free — `deadline_report` is pure, and deep research cannot call `company_deadlines`) with both full JSON documents in `metadata`; errors stay D-007 through the same `_tool_error`. D-024 ¶1 amended by D-031 (a sixth *registry* tool still needs an amendment). Declined: declining; a second `/mcp/chatgpt` endpoint (kept as the named escape hatch if eval E31 fails); a client-sniffed tool subset (MCP has no negotiated tool profile — a server can only guess at `clientInfo`). Spec, JSON examples, five eval cases (E27–E31, new group G) and README wording in `CONNECTOR_SPEC.md`. Footprint: `DECISIONS.md`, `CONNECTOR_SPEC.md`, one cross-reference line in `CORE_ROADMAP_SPEC.md`. No `src/` or `tests/` change; 423 passed / 5 deselected before, 424 after — the one extra is T24's, landing concurrently. | — |
| T23 | Sonnet | done | 2026-09-05 (commits dbf3c91, 208ce9a): D-031 implemented — `mcp/connector.py` (`search`/`fetch` aliases, verbatim D-031 descriptions, no country string in a branch), 31 connector tests, evals E27–E31 green, README "Add to ChatGPT" + "Add to Claude Desktop", `docs/clients.md` ChatGPT section, server card regenerated. Follow-up after a live check: `search("Equinor")` ranked a GB 0.8 hit above EQUINOR ASA (alphabetical fan-out on a confidence tie) → merged list sorted by confidence with an exact-name tie-break, capped at 10, spec amended. Tests 455. | — |
| T24 | Sonnet | done | 2026-09-05 (commit e58fab8): homepage playground on `static/index.html` — validate → lookup + deadlines, or search; request URL shown; install lines + badges under the result; no auto-fire; never touches 833286602. | — |
| T25 | Sonnet | done | 2026-09-05: Claude Code plugin marketplace (`.claude-plugin/marketplace.json` + plugin), MCPB manifest for Claude Desktop, `docs/clients.md` with per-client install snippets, one README pointer line. Verified live with the `claude` CLI: `claude plugin marketplace add foretak/registry-mcp` + `claude plugin install registry-mcp@foretak`; `mcpb validate` passes (icon 400 px, 512 recommended); `mcpb pack` builds a 2-file bundle. Gap: no privacy policy page yet — needed for the MCPB submission form and the Connectors Directory. | — |

## Phase 7 — Third country: Sweden, `registries/se/` (T26, started 2026-09-05 ~21:45 local on Kim's instruction "set up a team creating the Sweden folder"; brief in `tasks/T26.md`)

Denmark (T16) stays queued behind Erhvervsstyrelsen's answer (~2026-09-23); Sweden needs only a self-service credential form, so it goes first and Denmark reuses the recipe. Rules: zero `core/` edits (R-2 not authorised — REKLAMSPÄRR goes to `notes`; architect writes a recommendation for Kim), deadline rules ship only with a primary-source citation, no invented JSON shapes (VERIFY until live), no production calls.

| id | owner | status | done-check | blocker |
|---|---|---|---|---|
| T26r | Opus R | in progress | Recon: real API shape/OpenAPI, kundanmälan form URL + fields, check digit from a primary source, statutes (ÅRL 8:3, ABL 7:10, förseningsavgift, roll-forward), code lists, licence + rate-limit sentences → library `02b-sweden-api-shape-and-law.md` + `tasks/T26-recon.md`. | — |
| T26a | Opus A | in progress | `SWEDEN_SPEC.md` in `UK_SPEC.md`'s structure + D-032… (identifier, NAMNSKYDDSLÖPNUMMER, dual legal-form vocabulary, three-signal status, REKLAMSPÄRR→notes + R-2 recommendation, two-secret env contract, licence string, sole-trader note); §1/§5 finalised against T26r. | T26r for §1/§5 |
| T26b | Sonnet | queued | `registries/se/` + fixtures + tests per the numbered list; no-credentials path correct on both surfaces; local server check only. | T26a |
| T26c | Opus B | queued | CHANGELOG/README line/KEYWORDS §SE/docstring aliases — nothing that needs live output. | T26b |
| T26d | — | blocked | Go-live: Kim's Bolagsverket credentials on Railway → live fixtures, README example from the running server, 0.3.0 bump, deploy, release. | Kim: kundanmälan (`HUMAN_TODO.md` §7.7) |
| T26e | Opus A | queued | Review in `REVIEW.md` §T26e (T15e checklist scoped to SE). | T26b |
