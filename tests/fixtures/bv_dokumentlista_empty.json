{
  "_SYNTHETIC_COMBINATION": "SHAPE CONFIRMED, COMBINATION SYNTHETIC — the two keys below are exactly what Bolagsverket's OpenAPI document declares for `DokumentlistaSvar` (`dokument` is an array and is NOT in the schema's `required` list, so an absent key is as lawful as an empty one). The *combination* is synthetic because it cannot be recorded live: in the Bolagsverket TEST environment `/dokumentlista` has its own allowlist, and `5561890038` is the only number on it (verified 2026-09-08 — every other test-workbook number, including all eight companies with live `/organisationer` recordings, answers `/dokumentlista` with HTTP 400 `\"Ogiltig identitetsbeteckning i begaran. Se testdokumentation for giltiga identitetsbeteckningar.\"`, never a 200 with an empty list). So no company reachable from the test environment has zero filed annual reports, and DECISIONS.md D-041(h)'s rule — an empty list is a present block with `documents: []`, never `not_found` — is implemented from the ruling and the schema, not from an observed payload. Re-record against production the day that is lawful; see tests/fixtures/README.md.",
  "dokument": []
}
