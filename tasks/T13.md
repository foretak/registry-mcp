# T13 — Deploy verification and status page

Owner: Sonnet 4
Depends on: T10
Unblocks: T14

## Goal
No VPS exists yet, so verify the deployment story locally, end-to-end, exactly as `deploy.md` describes it — and fix `deploy.md` wherever reality differs.

1. `docker compose up --build -d` on this machine with `REGISTRY_MCP_DOMAIN=localhost` (Caddy will use its internal CA for localhost; document the `-k` / trust step).
2. `curl -k https://localhost/health` → 200. `curl -k https://localhost/v1/NO/company/923609016` → report. `curl -k https://localhost/llms.txt` → 200. MCP over HTTPS: `claude mcp add registry-mcp-docker --transport http https://localhost/mcp` and a `tools/list`.
3. Restart the stack; confirm the SQLite volume persisted (second lookup is `cached: true` after restart).
4. `GET /status` — a tiny public status page (HTML) showing: version, uptime, upstream brreg reachable (last check time), cache size, countries. No secrets, no per-query data. Add to `api/status.py`.
5. Update `deploy.md` with every correction; add a "smoke test" section with the curls above.

## Files you own
`api/status.py`, the include line in `api/main.py`, `deploy.md`, `docker-compose.yml`, `Dockerfile`, `Caddyfile`, `.env.example`, `tests/test_status.py`.

## Files you must not touch
`core/`, `registries/`, `mcp/`, `DECISIONS.md`.

## Done-check
All five steps above pass on this machine; paste the curl outputs into the `PROGRESS.md` done-check cell (abbreviated). `docker compose down -v` at the end. Set T13 to `review`.
