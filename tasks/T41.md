# T41 — Architect: D-045 — the charge shape ruled, the next depth sources ruled in or out, and the briefs for what is ruled in

Owner: Opus A (architect), dispatched by the orchestrator 2026-09-08. Worktree `~/registry-mcp-wt/t41`,
branch `t41`. Kim's instruction today: **"make the tasks and make it all live"** — the built depth is being
deployed by other agents this round; your job is what comes after it.

Read first: `DECISIONS.md` D-041, D-042, D-043, D-044; `REVIEW.md` § "D-044 wiring (`e03a518`)" (all
eight findings) and § "S-series" finding 4; `~/mcp-growth/DEPTH.md` (§1, §3, §4, §6 — the candidate
ranking and the closed doors, with primary sources fetched and cited); `CORE_ROADMAP_SPEC.md` §7 (R-5, the
LEI) and §8 (R-6, Peppol); D-026(c) (the `LeiRecord` shape) and D-029 (Peppol); `tasks/T31.md` Part B
§4–§6 (the Swedish deadline rung); `tasks/T38.md` (Norway `financials`, already briefed). Every fact you
need about the sources is in DEPTH.md with its provenance; fetch a primary source only to settle a
question it leaves open (VIES's REST shape and the Gazette's fair-use clause are two it names).

## Ground rules

- You write **one** `DECISIONS.md` entry, **D-045**, lettered parts, in the house style (decision, declined
  alternatives with reasons, "Applies to tasks" line). Append only. No other entry is amended except by
  name inside D-045.
- You may edit **`src/registry_mcp/core/models.py` docstrings only** (part (a) below). No other `src/` edit.
- You write task briefs under `tasks/` in the style of `tasks/T38.md`: ground rules, owned files, numbered
  work items, a done-check the orchestrator can run, an out-of-scope list. Implementers are Sonnets; write
  for one.
- No live calls to `api.foretak.dev`, Companies House or Bolagsverket. Keyless public calls (VIES, GLEIF,
  the Gazette, brreg) are fine for verification; record what you called in the entry.
- Never look up `833286602` or `833285602`. Never write a key anywhere.
- Commit on `t41`; do not merge, do not push. Do not edit `PROGRESS.md` or `REVIEW.md`.

## Constraints on the implementer tasks you brief (the orchestrator's partition for the next round)

Three Sonnets will run in parallel after this round: **T38** (Norway `financials`), **T42** (the LEI), and
**T45** (S-series leftovers in `mcp/server.py`, `api/dashboard.py`). Rules that keep them apart:

1. **Implementers do not touch `mcp/server.py`, `api/main.py` or the server card.** Tool prose,
   `_INCLUDE_DESCRIPTION`, card regeneration and README/llms-full/CHANGELOG are done **once per round** by
   a closing "surfaces sync" task. Say so in every brief; T38's "B4. Surfaces and prose" is overridden the
   same way (note it in T38's dispatch, do not rewrite T38).
2. `core/models.py` is shared. T38 appends its models after `InsolvencyBlock` and its `CompanyReport`
   field after `insolvency`; T42 inserts `LeiRecord` **immediately after `SourceRef`** and
   `CompanyReport.lei` **immediately before `charges`**. Different hunks, no conflict. Write those
   placements into T42's brief.
3. **T42 must not edit any `registries/*/__init__.py`** — T38 edits Norway's. Rule the mechanism that
   makes that possible (part (e)).

## The parts of D-045

**(a) The charge shape (S-series finding 4).** `core/models.py:600` `Charge` and `:675` `ChargeBlock` carry
thirteen field names that appear nowhere in `DECISIONS.md` (`charge_id`, `charge_number`, `created_on`,
`delivered_on`, `satisfied_on`, `status`, `is_outstanding`, `classification`, `assets_charged`,
`obligations_secured`, `contains_floating_charge`, `parties_entitled`, `total_count`, `outstanding_count`,
`satisfied_count` — check the file) and cite D-042(h), which rules `FiledDocument`. Rule the list: accept,
rename or strike each name against D-042(g)'s anti-bend rule and D-042(e)(3); answer the module's two
open questions (`outstanding_count = total_count − satisfied_count` when `part_satisfied_count` was 0 in
every payload; whether `part_satisfied_count` should exist); rule S-series finding 7 (free-text
`assets_charged`/`obligations_secured` relayed unbounded — a person-bearing risk D-042(e) never tested).
Then fix the two docstring citations in `core/models.py` to cite D-045(a). A rename that changes the wire
is a breaking change to a shipped 0.3.x block — say whether you are making one and why it is worth it.

**(b) VIES for Sweden.** DEPTH.md §4 names it as the one call that would answer the `vat_registered: null`
Sweden ships (SWEDEN_SPEC §2.6). It is a **second fetch**, so D-041(c) makes it an attachment, not a field:
rule the include name (country-neutral; "vat" is a candidate, argue it), the block shape, the `SourceRef`
(source, licence — VIES has terms, read them), TTL, and the empty states. **The hazard you must rule
explicitly:** a Swedish sole trader's VAT number is `SE` + personnummer + `01`, and VIES returns the
registered **name and address** for it — a natural person's. D-028, D-039 and D-040 bind. Say what the
block may carry for a registrant whose identifier `id_may_be_personal` flags (likely: the yes/no and
nothing else), and whether the lookup is refused outright for such identifiers. Also whether the block is
declared for GB/NO at all (VIES is EU-only; Northern Ireland `XI` aside, GB is not in it and NO never was).
Verify the REST endpoint shape with one keyless call on a company (not a sole trader) — DEPTH.md marks it
`[search]` only.

**(c) The FCA Financial Services Register (firm endpoints only).** Companies House's own
`partial_data_available` enum points at it. Rule: in or out for the next round; if in, the include name,
what the firm record may carry (no individuals — D-028), key handling (a self-serve key is a **second**
credential on the GB module — how `requires_api_key`/`api_key_env` express "the base lookup needs one key,
this attachment another"; D-017 and D-037 are the precedents), and the licence.

**(d) The Gazette.** Rule on the fair-use policy DEPTH.md quotes (5 requests / 10 s; "crawling in
non-business hours") — whether on-demand lookups are crawling; on the OGL personal-data exclusion; and
whether a notice block is worth a first tranche given `insolvency` now ships for GB. Likely "not now" —
if so, say why in one paragraph so it is not re-derived.

**(e) The LEI (R-5's payload), and the cross-country mechanism.** `CORE_ROADMAP_SPEC.md` §7 and D-026(c)
already rule the shape, TTL (7 days) and done-check. Confirm nothing in D-042/D-044 changed them. Then rule
the thing they did not foresee: an attachment **every** country declares. `supported_includes` is a
per-country `ClassVar`, and the method lives on the country module. Options: a concrete `lei` method on
the `Registry` base and a base-level `universal_includes = frozenset({"lei"})` that `lookup_with`,
`country_info()` and the misconfiguration guard all union with the country's set; or each country adds
`"lei"` by hand. The first keeps T42 out of every country module (constraint 3 above) and means a fourth
country gets the LEI for free; argue it or reject it. Also rule the **per-kind TTL table** in
`core/cache.py` — D-042(j) and D-043 both name it, three modules fake it with `status="not_found"`, and the
LEI's 7-day TTL is its first real user: shape the table (kind → (ok_ttl, empty_ttl)) and say which existing
callers migrate in T42 and which wait. Then write **`tasks/T42.md`** for a Sonnet: core-only footprint
(`core/models.py` at the placements above, `core/registry.py`, `core/cache.py`, `tests/`), GLEIF's
`lei-records?filter[entity.registeredAs]=` … — pin the exact query per country's identifier scheme from
GLEIF's documentation, with the country's registration-authority code (RA000472 Norway, RA000585 UK,
RA000544 Sweden — verify each against GLEIF's RA list before writing it down), the done-check from §7,
and the two-level nullability tested as such.

**(f) Peppol / ELMA (R-6).** D-029 and §8 are ruled and spec'd. Say whether a Sonnet can build it from
those two alone; if yes, write **`tasks/T43.md`** (Norway module + core model, no surfaces, same
constraints); if not, list exactly what is missing and stop there.

**(g) The Swedish deadline rung (REVIEW finding 2).** T31 Part B §4–§6 are unbuilt: `include=["filings"]`
on `company_deadlines` (both surfaces), rung 2 in `se/rules.py` from `report.filings.financial_year_end`,
N9's three states, the GB no-op. Confirm the brief is still right after D-044 (the block now exists and is
canonical), rule how a surface obtains a report with the block for the deadlines path (`lookup_with(id,
include)` then `deadlines(report, today)` — is anything needed in `core/registry.py`?), and write
**`tasks/T44.md`** as a pointer brief: "do T31 Part B §4, §5, §6 and its done-check; §1–§3 landed in
`e03a518`", plus the exact footprint (`registries/se/rules.py`, `tests/test_rules_se.py`, `SWEDEN_SPEC.md`,
and — because this one genuinely must touch the surfaces — `mcp/server.py` `company_deadlines` and
`api/main.py` `get_deadlines` **only**, in a round of its own after T38/T42/T45 merge).

**(h) One sentence each, so nobody re-derives them:** CCOD/OCOD, HMRC VAT, Charity Commission, Land
Registry price-paid, Employment Tribunal, OFSI, Norway's Løsøreregisteret / Konkursregisteret / Kunngjøringer /
`/roller` / reelle rettighetshavere, Sweden's `/dokument` zip / Skatteverket / Kronofogden — ruled out or
deferred, with the one reason each, citing DEPTH.md.

## Deliverables

`DECISIONS.md` D-045; `core/models.py` docstring fix for (a); `tasks/T42.md` (LEI) always; `tasks/T43.md`
(Peppol) if (f) says yes; `tasks/T44.md` (Swedish rung) always; a brief for the VIES block **only if** (b)
rules it in and the personal-data rule is settled — otherwise D-045(b) records why not. One commit per
file group is fine. Report: what you ruled, what you verified live, what you declined and why, and which
briefs exist.
