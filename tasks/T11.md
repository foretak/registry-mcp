# T11 — Packages, registries, GitHub hygiene

Owner: Opus B (growth)
Depends on: T10 (all fixes done)
Unblocks: T14

## Goal
Prepare everything for publication. **Do not actually publish to PyPI, npm, or any registry, and do not push to GitHub** — those need the human's accounts. Build, verify locally, and write exact commands into `SUBMISSIONS.md` and `HUMAN_TODO.md`.

1. **PyPI** — `pyproject.toml` metadata: description containing the keywords, `keywords`, classifiers, `urls`. `uv build`; verify `uvx --from dist/*.whl registry-mcp` starts a stdio server (send `tools/list`, get 5 tools back).
2. **Alias package** — `packages/brreg-mcp/pyproject.toml`: a package named `brreg-mcp` whose only dependency is `registry-mcp` and whose console script `brreg-mcp` calls the same entry point. Build it too.
3. **npm** — `packages/npm/registry-mcp/`: `package.json` (name `registry-mcp`, keywords, `bin`), a `bin/registry-mcp.js` that spawns `uvx registry-mcp` (fall back to `pipx run`, then print a clear install hint). Same for `packages/npm/brreg-mcp/`. Verify `node bin/registry-mcp.js` works locally.
4. **README.md** — top: one sentence with keywords, `claude mcp add` command, a 10-line real example output for 833286602, badges (PyPI, npm, CI — use the eventual URLs), then config snippets for Claude Code, Cursor, Claude Desktop, then the REST curl, then "Add your country" link to `CONTRIBUTING.md`.
5. **`CONTRIBUTING.md`**, `.github/ISSUE_TEMPLATE/` (bug, new-country), three seeded good-first-issue drafts in `.github/SEED_ISSUES.md`, repo topics list in `HUMAN_TODO.md`.
6. **`SUBMISSIONS.md`** — one section per registry (official MCP registry, Smithery, Glama, PulseMCP, mcp.so, MCP Market, awesome-mcp-servers + two other awesome lists): URL, required files (create them: e.g. `smithery.yaml`, `glama.json` if that's what they take — check each registry's current docs live), exact steps, and what needs a human login.

## Files you own
`pyproject.toml` (metadata only), `packages/`, `README.md`, `CONTRIBUTING.md`, `.github/ISSUE_TEMPLATE/`, `.github/SEED_ISSUES.md`, `SUBMISSIONS.md`, `HUMAN_TODO.md`, `smithery.yaml` and any other registry manifest.

## Files you must not touch
`core/`, `registries/`, `api/`, `mcp/`, `tests/`, `DECISIONS.md`.

## Done-check
```
uv build && uvx --from dist/registry_mcp-*.whl registry-mcp < tests/fixtures/tools_list.jsonl
cd packages/npm/registry-mcp && node bin/registry-mcp.js < ../../../tests/fixtures/tools_list.jsonl
```
Both print a `tools/list` response with 5 tools. `SUBMISSIONS.md` covers every registry named above. Set T11 to `review`.
