# T14 — Launch review and handoff

Owner: Orchestrator
Depends on: T11, T12, T13

## Goal
1. Re-run every done-check in `PROGRESS.md` from a clean clone (`git clone . /tmp/clean`). Anything that fails goes back to its original owner.
2. Roll `PROGRESS.md` completed phases into one-line summaries; keep the file under 100 lines.
3. Finalise `HUMAN_TODO.md`: domain + DNS, GitHub org/repo creation and push, PyPI/npm account + publish commands, registry logins (from `SUBMISSIONS.md`), article posting schedule (from `content/README.md`), the outreach list from `BRREG_MCP_FIRST_KRONE.md` (if that file still hasn't arrived, leave a clearly marked placeholder section).
4. Commit everything on `main`.
5. Freeze feature work. Write the five-line phase report for the human.

## Done-check
Clean-clone test suite green; `HUMAN_TODO.md` complete; final commit made.
