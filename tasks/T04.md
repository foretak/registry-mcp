# T04 — CI, Docker, Caddy, deploy doc

Owner: Sonnet 4
Depends on: T01
Unblocks: T13

## Goal
- `.github/workflows/ci.yml` — on push and PR: `uv sync`, `ruff check`, `mypy`, `pytest --cov`. Python 3.12. Cache uv.
- `.pre-commit-config.yaml` — ruff (lint + format), mypy.
- `Dockerfile` — multi-stage, `uv` builder → slim runtime, non-root user, `EXPOSE 8080`, `CMD` runs the API (`uvicorn registry_mcp.api.main:app --host 0.0.0.0 --port 8080` — confirm the exact module path from D-003). The API module may not exist yet: make the Dockerfile correct for the path decided in D-003 and add a `HEALTHCHECK` on `/health`.
- `docker-compose.yml` — services `api` (build ., volume `registry-data:/app/data`, env from `.env`) and `caddy` (ports 80/443, `Caddyfile` mounted, volumes for certs). `.env.example` with `REGISTRY_MCP_CONTACT_EMAIL`, `REGISTRY_MCP_ADMIN_KEY`, `REGISTRY_MCP_DOMAIN`.
- `Caddyfile` — `{$REGISTRY_MCP_DOMAIN}` reverse-proxying to `api:8080`; automatic HTTPS.
- `deploy.md` — exact commands for a fresh Ubuntu 24.04 VPS: install Docker, clone, `.env`, `docker compose up -d`, verify `/health`, how to view logs, how to update.

## Files you own
`.github/`, `.pre-commit-config.yaml`, `Dockerfile`, `.dockerignore`, `docker-compose.yml`, `Caddyfile`, `.env.example`, `deploy.md`.

## Files must not touch
Anything under `core/`, `registries/`, `api/`, `mcp/`, `tests/`, `pyproject.toml`, `DECISIONS.md`.

## Decisions that apply
D-001, D-003, D-006 (cache path must be on the volume).

## Done-check
```
docker build -t registry-mcp:dev .          # succeeds
docker compose config                       # valid
```
Because the API doesn't exist yet, the container is only required to build. Set T04 to `review` in `PROGRESS.md`; note that runtime verification is deferred to T13.
