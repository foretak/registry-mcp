# T60 — The T58 review's six non-gating findings: four unpinned invariants, the card's empty `resources`, and Sweden missing from two argument descriptions

Owner: a Sonnet, dispatched by the orchestrator 2026-09-09 after the 0.4.0 deploy. Worktree `~/registry-mcp-wt/t60`,
branch `t60`, cut from `main` after the T58 fixes. Ships in **0.4.1** together with T59.

Read first: `REVIEW.md` § "Depth track `e03a518..81c91e7` — 2026-09-09 — APPROVED WITH FIXES" — its "Mutation
results" table (four mutations the suite did not catch), findings **2, 3, 4, 5, 7, 8** (each names the file, the
test and the size), and the "Not on the before list — a Sonnet follow-up, sized" table. Then `DECISIONS.md`
D-043(e),(f), D-044(b), D-047(f). Findings 1 and 6 are already applied on `main`; do not touch them.

## Ground rules

`uv sync --locked --all-extras`; `uv run pytest -m "not live" -o addopts="" -p no:cacheprovider` (never `-q`),
`uv run mypy .`, `uv run ruff check .`; counts before and after. Never `ruff format` unscoped. No live calls at
all — every fixture is hand-built or already committed. Never look up `833286602` or `833285602`. Commit on `t60`
at least every 30 minutes, one commit per finding; do not merge or push; do not edit `PROGRESS.md`, `REVIEW.md`,
`DECISIONS.md`, `HUMAN_TODO.md`, `tasks/`.

## The six items, in the review's order

2. **`liabilities` and `total_comprehensive_income` are `None` unconditionally for Sweden, and no test notices
   when they stop being** (the review derived `liabilities = current + non_current` and 1030 still passed). One
   test in `tests/test_client_se.py` asserting both are `None` on both real K2 fixtures *while* the two summands
   are present. Prove it red first: apply the review's mutation to `registries/se/financials.py`, watch the test
   fail, restore.
3. **Norway's `filings` scope note is unpinned** — deleting it, or appending it instead of inserting it first,
   changes nothing in the suite. Add the NO case of `test_scope_note_is_present_first_on_every_block_d044b`
   in `tests/test_client_no.py`, both the filled branch and the empty branch. Prove it red the same way.
4. **The currency-unresolved branch has no fixture.** One hand-built iXBRL fixture whose unit is not an ISO 4217
   measure (say `xbrli:pure` on every fact), plus one test asserting `periods == []` and the
   `_CURRENCY_UNRESOLVED_NOTE` in `notes`; then mutate the extractor to default to `"SEK"` and watch it go red.
   The fixture carries no name, no personnummer and nothing copied from a real filing (`tests/fixtures/README.md`
   gains a row saying it is hand-built and why).
5. **Which half of the `ix:nonFraction`-only guarantee is load-bearing**: one sentence in the docstring of
   `test_d047_f3_…` (find it) saying that the runtime half cannot catch a breach (a `nonNumeric` fact carries no
   `@format`, so `_parse_number` drops it silently) and the AST assertion is the whole guard.
7. **The card publishes `"resources": []` while the server serves three.** Extend `scripts/regen_server_card.py`
   to write the resource list (uri, name, description, mimeType — whatever `Client.list_resources()` returns) and
   extend `test_server_card_tools_and_prompts_match_the_live_server` in `tests/test_mcp.py` to compare them, the
   way it compares prompts; run the script; commit the regenerated card. A one-character mutation of a resource
   description must turn the test red.
8. **`_COUNTRY_DESCRIPTION` and `_ID_DESCRIPTION` in `mcp/server.py` name two of three countries.** Add the
   Swedish clause to each (identifier: ten digits, or twelve for a sole trader — read `registries/se/` for the
   exact words already used elsewhere) and `"SE"` to `_COUNTRY_EXAMPLES`; regenerate the card; the drift tests
   must pass. Keep it to one clause each — T59 is about to put every tool description on a budget.

## Done-check (the orchestrator verifies)

- Each of the four mutations the review listed as "not caught" now turns at least one test red (name the test
  per mutation in your report, with the red output).
- The card's `resources` entry lists three resources and the drift test compares them.
- `lookup_company`'s `country` and `id` argument descriptions in the card name Sweden.
- Suite, mypy, ruff clean with counts; `git diff --stat` inside: `tests/test_client_se.py`, `tests/test_client_no.py`,
  `tests/test_mcp.py`, `tests/fixtures/` (one new fixture + README row), `scripts/regen_server_card.py`,
  `src/registry_mcp/mcp/server.py` (the two description constants and `_COUNTRY_EXAMPLES` only),
  `static/well-known/mcp/server-card.json`, one test docstring.
