# T40 — One shape per concept: fold the four country stand-ins into `core.models`, and put the scope note on every `filings` block

Owner: a Sonnet, dispatched by the orchestrator 2026-09-08. Worktree `~/registry-mcp-wt/t40`, branch `t40`.
Source: `REVIEW.md` § "D-044 wiring (`e03a518`)" findings **1** (blocking) and **4**, plus S-series finding 10
and the `charges.py` half of S-series finding 4. Read those, then `DECISIONS.md` D-044 (all of it — (a) is the
measurement that makes this task a rename), D-042(b) (the sentence declining "two shapes for one concept"),
D-042(g), D-041(c).

## Ground rules

- `uv sync --locked` first. Then `uv run pytest -m "not live" -o addopts=""` (never `-q`), `uv run mypy .`,
  `uv run ruff check .`. Baseline at `e03a518`: **834 passed, 22 deselected**. Report counts before and after
  and account for every change in the count.
- **Never run `ruff format` unscoped** — only on `.py` files you changed.
- **No live calls to `api.foretak.dev`, Companies House, Bolagsverket or brreg.** Every fixture you need is
  committed. Never write an API key anywhere. Never look up `833286602` or `833285602`.
- Commit on branch `t40`; do not merge, do not push. Do not edit `PROGRESS.md`, `REVIEW.md`, `DECISIONS.md`,
  `HUMAN_TODO.md`, or any task file.

## Files you own

`src/registry_mcp/registries/gb/filing_history.py`, `gb/insolvency.py`, `gb/charges.py`, `gb/client.py`
(return-type annotations only), `gb/__init__.py`; `registries/se/filings.py`, `se/client.py` (same),
`se/__init__.py`; `registries/no/accounts.py`, `no/client.py` (same), `no/__init__.py`;
`tests/test_attachments.py` (**only** the last section, "The three real attachments, wired through the
seam"), `tests/test_client_gb.py`, `tests/test_client_se.py`, `tests/test_client_no.py`; `UK_SPEC.md`,
`SWEDEN_SPEC.md`, `NORBIZ_SPEC.md` only where they name a stand-in class by name.

**Do not touch `src/registry_mcp/core/`, `src/registry_mcp/api/`, `src/registry_mcp/mcp/`, the server card,
`tests/test_mcp.py`, `tests/test_api.py`, `README.md`, `CHANGELOG.md`, `static/`.** Another agent (T39) owns
those this round. If this task needs a `core/models.py` edit, **stop and report** — do not make it.

## 1. The scope note, on every call (finding 1 — blocking; do this first and commit it separately)

D-044(b) rules one include name for three scopes *because* "the scope difference is disclosed in the block's
own `notes`, on every call"; `mcp/server.py` and `core/models.py:857` publish that promise. Measured:
the Swedish block returns three documents and `notes: []`; the British block's notes never name the scope.
Norway's `_ONE_PERIOD_NOTE` (`no/accounts.py`) is the pattern — copy the pattern, not the text.

- `se/filings.py`: a module constant `_SCOPE_NOTE`, appended in `map_dokumentlista` **unconditionally**
  (empty or not, calendar year or not), as the **first** note. In substance: Bolagsverket's document list
  publishes filed annual reports only, not a general filing history; other filings the company has made
  (board changes, articles, capital) are not listed here and their absence means nothing.
- `gb/filing_history.py`: `_SCOPE_NOTE`, appended unconditionally, first. In substance: Companies House
  publishes the whole filing history here — every category of filing, not only accounts — so an accounts
  filing is one row among confirmation statements, officer changes, charges and the rest; use `category`
  and `kind` to pick the rows you mean.
- Wording: plain English, one or two sentences, the register named, no legal citation needed. It must be
  true of the wire as the module already documents it — read the module docstring before writing.
- Tests: in `tests/test_client_se.py` and `tests/test_client_gb.py`, one test each asserting the note is
  present on a non-empty block **and** on an empty block (use the existing fixtures and the empty-payload
  cases the modules already test). Do not weaken any existing assertion; if an existing test asserts
  `notes == []` on one of these blocks, update it and say so in your report.

## 2. Fold the stand-ins into `core.models` (finding 4)

At `e03a518` the tree holds `SourceRef` **plus** `ChargeProvenance`, `InsolvencyProvenance` and three
`FilingProvenance`; `FiledDocument` ×4, `FilingHistory` ×4, `InsolvencyEvent`/`InsolvencyCase` ×2 (and
`InsolvencyList` beside `InsolvencyBlock`), `Charge` ×2 (and `ChargeList` beside `ChargeBlock`). D-044(a)
established by measurement that they are field-for-field identical; D-042(b) declined by name "two shapes
for one concept". The registry methods convert on every call with `model_validate(model_dump())` and a
drift test polices the copies. End state: **one class per concept, in `core/models.py`, and no conversion.**

For each of the five modules:

- Import the canonical classes from `registry_mcp.core.models` (`FiledDocument`, `FilingHistory`,
  `SourceRef`; `InsolvencyEvent`, `InsolvencyCase`, `InsolvencyBlock`; `Charge`, `ChargeBlock`) and delete
  the module's own `_Base`, `*Provenance`, `FiledDocument`, `FilingHistory`, `InsolvencyEvent`,
  `InsolvencyCase`, `InsolvencyList`, `Charge`, `ChargeList`. Mappers construct the canonical classes
  directly (`SourceRef(...)` where `FilingProvenance(...)` was; `InsolvencyBlock(...)` where
  `InsolvencyList(...)` was; `ChargeBlock(...)` where `ChargeList(...)` was). `client.fetch_*` return the
  canonical types. Update `__all__`.
- **Before deleting a class, diff its field descriptions against the canonical one.** D-044(a) says every
  fact was kept when the docstrings moved. Verify that. If a module description carries a fact (a count,
  a wire observation, a citation) the canonical description lacks, **do not edit `core/` — list it in your
  report, verbatim, with the field name**, and carry on. The orchestrator will route it.
- `gb/filing_history.py`'s `FilingHistory` had `total_count`; the canonical one has it too, default
  `None`. Sweden's and Norway's mappers simply do not set it (D-011: the register publishes no count).
- The three `registries/*/__init__.py` attachment methods (`filings` ×3, `insolvency`, `charges`) become
  `return await client.fetch_x(id)` (through the local import they already use). Keep their docstrings
  but delete the paragraph that begins "The module behind the seam was written blind to
  `core/models.py`'s shapes on purpose" — it is no longer true — and say instead that the module builds
  the canonical block directly. Every other sentence in those docstrings stays.
- Module docstrings that describe the stand-in arrangement ("Stand-in for the future
  `core.models.ChargeBlock`", "wiring this up once R-5 lands is a rename", the "parallel-work seam"
  paragraphs) are rewritten to describe the present: the module maps the wire onto the canonical models.
  Keep every wire fact and every citation that is still true.
- **`gb/charges.py` citations (S-series finding 4, the half that lives in your footprint).** The module
  says in eight places that its field list is "copied field-for-field from `DECISIONS.md` D-042 part (h)
  — the ruled shape of the future `core.models.Charge`". D-042(h) rules `FiledDocument`; no charge field
  name appears anywhere in `DECISIONS.md`. Replace every such citation with the honest one: the shape was
  *shaped by* D-042(g)'s anti-bend rule and D-042(j)'s truncation and derived-flag rules, and the field
  list itself is this module's proposal, pending the architect's ruling (a D-045 entry is being written
  this round; cite it as "D-045, pending"). Keep the module's two honestly flagged open questions
  (`outstanding_count` arithmetic, `part_satisfied_count`) exactly as they are.
- **`gb/charges.py` recon numbers (S-series finding 10).** Lines ~80, ~87 and ~284 say "39 items on four
  companies" and name Monzo (`09446231`). Recount from the four committed charge fixtures
  (`tests/fixtures/ch_*_charges.json`): items, `persons_entitled` values, distinct values, and which four
  companies they are. Write the real numbers and the real company list; drop Monzo from the list (no
  charges fixture for it is committed, and you may not record one).

### Tests

- `tests/test_attachments.py`, last section only: delete
  `test_filings_conversion_preserves_every_field_in_every_country` and
  `test_gb_insolvency_conversion_preserves_every_case_and_event` — they test a conversion that no longer
  exists. **Keep** `test_lookup_with_routes_each_block_to_the_field_of_the_same_name` (routing is still a
  real risk) and `test_no_practitioner_field_survives_the_conversion_to_core_models` — rename it to
  `test_no_practitioner_field_exists_on_the_canonical_insolvency_models` and reword its docstring: the bar
  holds because the model has no field for a practitioner particular, and the mapper strips them before
  constructing it. Rewrite the section's header comment to match: one shape, no seam, routing still asserted.
- `tests/test_client_gb.py`, `test_client_se.py`, `test_client_no.py`: fix imports that named the deleted
  classes; assertions that compared `isinstance(x, module.FiledDocument)` now compare against the
  canonical class. **No assertion is weakened.** Any test that existed to prove "the stand-in equals the
  core shape" is deleted with the stand-in and named in your report.
- Expect roughly `834 − 2 + 2 = 834`; report the exact figure and list every test added or removed.

## Done-check (the orchestrator verifies)

- `grep -rn "^class .*Provenance\|^class FiledDocument\|^class FilingHistory\|^class Insolvency\|^class Charge\b\|^class ChargeList\|^class _Base" src/registry_mcp/registries/` → **empty**.
- `grep -rn "model_validate(.*model_dump" src/registry_mcp/registries/` → **empty**.
- `grep -rn "D-042(h)\|D-042 part (h)" src/registry_mcp/registries/gb/charges.py` → **empty**.
- `grep -rn "09446231" src/registry_mcp/registries/gb/charges.py` → **empty**.
- Running `map_dokumentlista` and `map_filing_history` on their fixtures, and on an empty payload, yields a
  block whose `notes[0]` is the scope note (a test proves it for each).
- `git diff --stat` shows nothing under `core/`, `api/`, `mcp/`, `static/`, and nothing in `tests/test_mcp.py`.
- Suite green; counts reported and reconciled; `mypy .` clean; `ruff check .` clean.
- Two commits on `t40`: first the scope notes (so it can be cherry-picked alone if needed), then the fold.
