# T49 — Architect: D-046 amends D-029 on the four points `tasks/T48-recon.md` established, decides the DNS resolver, and writes `tasks/T43.md`

Owner: Opus A (architect), dispatched by the orchestrator 2026-09-08. Worktree `~/registry-mcp-wt/t49`, branch `t49`.
Read first: **`tasks/T48-recon.md`** (all of it — 458 lines, every fact fetched and counted), then `DECISIONS.md`
D-029 (the ruling being amended), D-045(f) (why the recon was needed), D-045(e) (the `universal_includes`
mechanism — Peppol is Norway-only, so it is a country declaration, not a universal one; say so), D-041(c) and
D-043(h) (one fetch, one `SourceRef`; here it is two fetches — DNS then HTTPS — for one answer, which needs a
sentence), `CORE_ROADMAP_SPEC.md` §8, `tasks/T42.md` (the shape of a core-plus-one-module brief this round).

## Ground rules

- One `DECISIONS.md` entry, **D-046**, house style, append only; amend D-029 by name and part. You may edit
  `CORE_ROADMAP_SPEC.md` §8 to match. No `src/` edits. Write `tasks/T43.md` for a Sonnet in the style of
  `tasks/T42.md`: footprint, work items, done-check, out of scope; implementers do not touch `mcp/server.py`,
  `api/main.py` or the card (T46 does).
- Keyless public reads only if you must re-verify something; T48 already resolved Equinor live. Never call
  `api.foretak.dev`, Companies House or Bolagsverket. Never look up `833286602` or `833285602`.
- Commit on `t49`; do not merge or push. Do not edit `PROGRESS.md` or `REVIEW.md`.

## What D-046 must rule

1. **`registered: false` is earnable only on the SML/SMP route.** A Peppol Directory miss is `null`, never
   `false` — both operators say a Directory absence means nothing. The Directory may answer `true` and fill
   `document_types`. Rewrite D-029(b)'s "False is an honest value" and D-029(d)'s fallback accordingly.
2. **`provenance.source` names the SMP host that answered, read from the NAPTR**, never a constant — 42 of 43
   Norwegian participants resolved to `smp.elma-smp.no` and one to `smp.conta.no`. The SMP base URL is data.
   Rule that a hardcoded host is a defect, and that the EC SML zone / `B-<md5>` CNAME form is dead (T48 §trap).
3. **The DNS resolver.** The SML step reads a U-NAPTR record; the stdlib cannot. Choose between `dnspython` as a
   sixth runtime dependency and DNS-over-HTTPS via the existing `httpx` (which puts Google or Cloudflare in the
   trust path of a statutory answer). Argue it; rule it; say what a self-hoster on a locked-down network sees in
   either case and how the block degrades (`registered: null`, `participant_id` still populated — D-029's rule).
4. **TTL is ours.** Both upstreams send `no-store`; the NAPTR TTL is 60 s. Cache the composed answer under the
   per-kind TTL table T42 built (`peppol` → 24 h positive / 1 h negative per D-029(e)), never the NAPTR→SMP
   binding. Confirm the kind name and the key shape (`NO:brreg:peppol:{orgnr}`? — the third segment is the kind).
5. **`document_types` vs process identifiers.** Document type ids come from the ServiceGroup (one call);
   process ids need one call per document type. Rule what the block carries in the first tranche (T48 suggests:
   the document type ids only, plus a derived `can_receive_invoice: bool | None` by membership of a committed
   table containing the BIS Billing 3.0 Invoice id — D-042(j)'s derived-flag rule; DFØ: "Peppol BIS billing v3.0
   er det samme som EHF-faktura").
6. **The Directory's Privacy Policy** binds the `contact` block — rule that it is never read or relayed (D-028).
7. **Licence.** data.norge.no lists ELMA recipients with "Lisens: Ikke oppgitt"; the Directory states none. Rule
   what `SourceRef.license` carries (likely `None` with a `notes` sentence naming the absence — D-011) and whether
   `HUMAN_TODO.md` gets an item asking Digdir. Also correct D-029(a)'s quote that no longer exists on DFØ's page
   (T48 records the replacement evidence).
8. **Sequencing.** Peppol is Norway-only, keyless, person-clean. Say where T43 sits relative to T44/T47 and
   whether it can share a round with them (footprint: `registries/no/peppol.py` new, `registries/no/__init__.py`,
   `core/models.py` `PeppolParticipant` + `CompanyReport.peppol`, `core/cache.py` kind row, tests, and — if you
   choose it — `pyproject.toml` for `dnspython`).

## Deliverables

`DECISIONS.md` D-046; `CORE_ROADMAP_SPEC.md` §8 updated; `tasks/T43.md`. Report: the eight rulings in one
sentence each, the resolver decision and its reason, and the one thing T43's implementer is most likely to get
wrong.
