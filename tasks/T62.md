# T62 — `rules last reviewed` stamp: every computed deadline and every rules resource says when its statutes were last checked

Owner: a Sonnet, dispatched by the orchestrator 2026-09-10. Worktree `~/registry-mcp-wt/t62`, branch `t62`, cut from
`main`. Ships in 0.4.2 (no version bump in this task).

Why: the products research round (`~/mcp-growth/products/00-SYNTHESIS.md` §4.1 S1, from `01-assets-inventory.md`
F7 and `~/mcp-growth/DECISION-GATE.md` §7) found the one change every verdict wants: a deadline engine that cites a
statute beside a date becomes *confidently wrong* the day the statute changes, and today nothing tells a caller
when the rules were last checked. If the project is parked at the day-45 gate, this is the stamp that keeps the
answers honest as they age.

Read first: `DECISIONS.md` D-018, D-022, D-023 (what a deadline cites and why), D-041 (the two rungs);
`src/registry_mcp/core/models.py` (`Deadline`, `DeadlineReport`), `src/registry_mcp/registries/{no,gb,se}/rules.py`
(each `rules_markdown()` and the `applies_because` builders), `tests/test_rules_*.py`, `tests/test_mcp.py` (the card
drift tests — the card regenerates via `scripts/regen_server_card.py`).

## Ground rules

`uv sync --locked --all-extras`; `uv run pytest -m "not live" -o addopts="" -p no:cacheprovider` (never `-q`),
`uv run mypy .`, `uv run ruff check .`, counts before and after. Never `ruff format`. No live calls. Never look up
`833286602` or `833285602`. Commit on `t62` at least every 30 minutes; do not merge or push; do not edit the board
files (`PROGRESS.md`, `REVIEW.md`, `DECISIONS.md`, `HUMAN_TODO.md`, `tasks/`).

## The change (small — an hour or two; if it grows past that, stop and report why)

1. **One constant per country module**, `RULES_LAST_REVIEWED = date(2026, 9, 10)` in each `registries/<cc>/rules.py`
   (Norway's statutes were last verified by R01/T18 on 2026-09-05, Britain's by T15 on 2026-09-04, Sweden's by
   T26/T44 on 2026-09-08 — use **those** dates, not today's, unless you re-verify a statute yourself; say which in
   the report).
2. **`DeadlineReport` gains `rules_last_reviewed: date`** (shared model, additive, described: "the date this country's
   deadline rules — the statutes and their day-count arithmetic — were last checked against the law; a deadline
   computed long after this date should be re-verified before anyone acts on it"), filled by
   `Registry.deadline_report`/`deadline_report_with` from the module constant. Place the field after `notes`. If
   `Deadline` itself is the better home (per D-018's "each deadline states its own basis"), argue it in the report and
   do it there instead — one place, not both.
3. **`registry://rules/{country}`** (`rules_markdown()`) opens with one line: "Rules last reviewed: YYYY-MM-DD".
4. **`company_deadlines`' docstring and the REST route description** gain one sentence naming the field. Regenerate
   the card. Both card drift tests must pass; the REST≡MCP parity test for deadlines must still pass.
5. **Tests**: one per country asserting the field equals the module constant; one asserting the rules resource opens
   with the line; the model field pinned in the card's `outputSchema` (the drift test covers it).
6. **CHANGELOG** `[Unreleased]`: one *Added* line, citing this file and the synthesis.

## Done-check (the orchestrator verifies)

- `GET /v1/NO/company/923609016/deadlines` from a local server carries `rules_last_reviewed`; MCP `company_deadlines`
  returns the same; the three rules resources open with the line.
- Suite, mypy, ruff clean with counts; card regenerated; `git diff --stat` inside: the three `rules.py`,
  `core/models.py` (one field), `core/registry.py` (the fill), `mcp/server.py` and `api/main.py` (one sentence each),
  the card, tests, CHANGELOG.
