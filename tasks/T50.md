# T50 — The Anthropic track: the Connectors Directory submission package, and the Claude Code plugin rebuilt as a job

Owner: Opus (growth), dispatched by the orchestrator 2026-09-08 on Kim's "the anthropic track as well". Worktree
`~/registry-mcp-wt/t50`, branch `t50`. Read first: `~/mcp-growth/ADOPTION.md` Finding 3 and ranked actions 2 and 3
(the evidence and the two tracks, with first-party citations), `SUBMISSIONS.md` (every listing audited on
2026-09-07 — the house record of what was claimed where), `~/mcp-growth/DISCOVERY.md` (the 2026-09-08 correction:
directory copy is hygiene, and ranking is usage-based), `README.md`, `docs/clients.md`, `legal/privacy.md`,
`legal/terms.md`, `plugins/` and `.claude-plugin/`, `src/registry_mcp/mcp/server.py` (tool titles, annotations,
`instructions`), and the standing constraints in `PROGRESS.md`'s header.

## Ground rules

- **Agents do not post to forums, social media, e-mail or chat, and do not buy anything.** Directories and list
  repositories are allowed. The Connectors Directory submission requires a Team/Enterprise organisation Kim
  does not yet have; the plugin submission form requires Kim's Console login. **You prepare everything; Kim
  submits.** Your deliverable is a package Kim can paste in ten minutes, and a HUMAN_TODO item that says so.
- No live calls to `api.foretak.dev` beyond unlogged static routes (`/health`, `/icon.png`, `/legal/privacy`,
  `/legal/terms`, `/.well-known/mcp/server-card.json`, `/llms.txt`) — those you may read to verify a URL resolves.
  Never look up `833286602` or `833285602`. Never write a key anywhere.
- Commit on `t50`; do not merge or push. Do not edit `src/`, `PROGRESS.md`, `REVIEW.md`, `DECISIONS.md`, or any
  task file. `HUMAN_TODO.md`, `SUBMISSIONS.md`, `content/`, `plugins/`, `.claude-plugin/` and `docs/clients.md`
  are yours.

## Track 1 — Connectors Directory submission package (ADOPTION §5.2)

Write **`content/anthropic-connectors-submission.md`**: every field the submission portal asks for, pre-filled,
in the order the portal asks (read <https://claude.com/docs/connectors/building/review-criteria> and the
directory docs it links; cite the page for each requirement). At minimum:

- **Name** — the job, not the architecture; consistent with the Smithery rename ("Company Check — UK, Sweden &
  Norway business registers" or better; argue the choice in one line and check it against the directory's
  naming rules). **Never "registry-mcp".**
- Short and long descriptions written as the task a user has; the six attachments named in plain words once
  0.4.0 is out (`filings`, `charges`, `insolvency`, `financials`, `lei`, `peppol` — check `PROGRESS.md` Phase 8
  for which are merged at the time you write; describe only what is merged, and mark the rest "after 0.4.0").
- Server URL (`https://api.foretak.dev/mcp`), transport, auth mode ("no authentication"), the privacy policy
  URL and terms URL (verify both resolve), icon URL, docs URL, support contact (`hello@foretak.dev`), category.
- **Reviewer test instructions**: three exact calls with expected results, using the documented example
  identifiers (`923609016`, `00445790`, `5560160680`) — the ones already in every README example.
- **The "Data handling" answer** for the API-ownership criterion: we proxy three government registers under
  their published terms — NLOD 2.0 (Brønnøysundregistrene), Crown copyright / Companies House terms,
  Bolagsverket's värdefulla datamängder terms, GLEIF CC0 — quoted with URLs, plus what we log (D-040: never a
  personal identifier) and retain (12 months, per `legal/privacy.md`). Precedents to name: Pappers, Firmenbuch,
  D&B.
- **A review-criteria checklist**, item by item, each marked PASS with the evidence (file:line or URL) or
  OPEN with what Kim must do. Known PASS: `title` and `readOnlyHint` on every tool, HTTPS Streamable HTTP,
  privacy policy served (commit `0d20df9`). Known OPEN: the Team organisation.

Then a **`HUMAN_TODO.md` §7.9** block: buy Claude Team (two seats, ~$50/month, cancellable — cite the pricing
page as of today), the portal URL, "paste `content/anthropic-connectors-submission.md`", and the one decision
Kim must make (the listing name). Add the same to `SUBMISSIONS.md` as a pending row.

## Track 2 — The Claude Code plugin as a job (ADOPTION §5.3)

`plugins/registry-mcp/` is a bare `.mcp.json`. Anthropic's guidance says a good plugin solves a job end to end.
Rebuild it: a `SKILL.md` that teaches the counterparty-check / filing-deadline / register-coverage workflow
(mirror the three MCP prompts' logic — read them in `mcp/server.py`; the caveat constant `_PAYMENT_FRAUD_CAVEAT`
must appear verbatim), and three slash commands — `/check-supplier`, `/filing-deadlines`,
`/enrich-company-list` — each a short markdown command that calls the hosted tools with `include` where it helps.
Run `claude plugin validate` and record its output. Update `.claude-plugin/marketplace.json` if the plugin's
description changes. Then a HUMAN_TODO line: submit at `https://platform.claude.com/plugins/submit` (free,
individual Console login), with the caveat ADOPTION records (it may land in the community marketplace, not the
official one — say which you believe and why).

## Done-check (the orchestrator verifies)

- `content/anthropic-connectors-submission.md` exists; every URL in it returns 200 (you list them with codes);
  every claim about the server is true of `main` at the commit you branched from (name the commit).
- `claude plugin validate plugins/registry-mcp` passes (output pasted in the report).
- `grep -c "registry-mcp" content/anthropic-connectors-submission.md` — appears only as the package/repo name,
  never as the listing name.
- `HUMAN_TODO.md` §7.9 and `SUBMISSIONS.md` updated; nothing under `src/` touched.
