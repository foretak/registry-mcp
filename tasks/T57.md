# T57 — The nine Swedish `_VERIFY` fixtures become real TEST-environment recordings

Owner: a Sonnet, dispatched by the orchestrator 2026-09-09. Worktree `~/registry-mcp-wt/t57`, branch `t57`.
Why now: `~/secrets/registry-mcp/bolagsverket-test.txt` has held the TEST credentials since 2026-09-07 (the board's
"zip still not downloaded" was stale), and T55 has just merged, so nobody else is in `registries/se/` or its tests.

Read first: `tests/fixtures/README.md` (the Swedish section — which fixtures carry `_VERIFY` / `_SYNTHETIC_COMBINATION`
headers and what each was meant to prove), `SWEDEN_SPEC.md` §1 (the TEST allowlist of identifiers — `/dokumentlista`
has its own), `tasks/T26-recon.md` and `tasks/T26.md` (how the first recordings were made), `registries/se/client.py`
(the `BOLAGSVERKET_ENVIRONMENT` switch selecting the TEST hosts), `DECISIONS.md` D-037, D-039, D-040.

## Ground rules

- `uv sync --locked --all-extras`; `uv run pytest -m "not live" -o addopts=""`, `uv run mypy .`, `uv run ruff check .`;
  record the baseline at your branch point and the counts after. Never `ruff format` unscoped.
- **Live calls: Bolagsverket TEST environment only**, credentials read from `~/secrets/registry-mcp/bolagsverket-test.txt`
  into shell variables inside the recording script — never echoed, never written, never committed; the recording
  script itself is committed under `tests/fixtures/` only if it already has a home there (check), otherwise it stays
  in your scratch directory and its exact commands go into `tests/fixtures/README.md`. Production credentials are not
  used. No call to `api.foretak.dev`, Companies House or brreg. Never look up `833286602` or `833285602`.
- **D-039 / D-040**: a Swedish sole trader's identifier is a personnummer. The TEST environment's sole-trader
  identifiers are synthetic test data, but the rule is the rule: **no fixture may be committed that carries a natural
  person's name or a real personnummer** — if a TEST payload for an enskild näringsidkare carries a name, the fixture
  keeps the shape and replaces the name with a marked placeholder, and the README says so.
- Commit on `t57`; do not merge or push. Do not edit `PROGRESS.md`, `REVIEW.md`, `DECISIONS.md`, `HUMAN_TODO.md`, `tasks/`.

## The work

1. List the nine (or however many — count them) Swedish fixtures marked `_VERIFY` or `_SYNTHETIC_COMBINATION`, and
   for each: the identifier it should be recorded from (from the README / spec allowlist), and the state it must
   show (active AB, avregistrerad, dormant, fusion, konkurs, rekonstruktion, BRF, EK, HB, enskild ×2, SCB-only, the
   error shapes 400/401/403/500, `finns_ej`, `uppgiftskalla_fel`, `dokumentlista_*`).
2. Record each one live against TEST. Where a state cannot be produced on TEST (an error shape, a combination the
   allowlist does not contain), keep the synthetic fixture, keep its header, and say in the README why it stays
   synthetic — do not fake a recording.
3. Replace each recorded fixture in place; drop the `_VERIFY` marker; update the README rows with the date, the
   identifier and the environment. Every test that consumed the old fixture must still pass; where a real payload
   differs from the synthetic guess (a key name, a value, a spelling like `pagaende`), **fix the test's expectation
   to the wire and report the difference** — that is the point of the task.
4. If a real payload reveals a mapping defect (the way T26g and T33 did), do not fix the mapper: report it with the
   fixture and the failing assertion, and leave the test marked `xfail` with the reason.

## Done-check (the orchestrator verifies)

- `grep -rln "_VERIFY" tests/fixtures/` lists only fixtures the README explains cannot be recorded on TEST.
- `grep -rn "personnummer\|[0-9]\{8\}-[0-9]\{4\}" tests/fixtures/bv_enskild*.json` shows no real personal identifier —
  the README documents the placeholder rule applied.
- Suite green with counts; mypy and ruff clean; `git diff --stat` limited to `tests/fixtures/`, `tests/test_client_se.py`,
  `tests/test_rules_se.py`, `tests/fixtures/README.md`, `SWEDEN_SPEC.md` (only if a spec claim is corrected by the wire).
- A table in your report: fixture → identifier → recorded/kept-synthetic → what differed from the synthetic version.
