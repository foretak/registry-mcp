# T39 — Ship-blockers on the surfaces and docs: the card's `inputSchema`, REST's `include` text, llms-full/README, CHANGELOG

Owner: a Sonnet, dispatched by the orchestrator 2026-09-08. Worktree `~/registry-mcp-wt/t39`, branch `t39`.
Source: `REVIEW.md` § "D-044 wiring (`e03a518`)" findings **5, 6, 7**. Read that section first, then
`DECISIONS.md` D-042(d) (discoverability is paid in description text, not only the schema enum) and D-044(b).

## Ground rules

- `uv sync --locked` first. Then `uv run pytest -m "not live" -o addopts=""` (never `-q`), `uv run mypy .`,
  `uv run ruff check .`. Baseline at `e03a518`: **834 passed, 22 deselected**. Report counts before and after.
- **Never run `ruff format` unscoped** — only on `.py` files you changed.
- **No live calls to `api.foretak.dev`, Companies House or Bolagsverket.** Nothing here needs the network.
- Never write an API key anywhere. Never look up `833286602` or `833285602`.
- Commit on branch `t39` with a message that says what changed and why; do not merge, do not push.
- Do not edit `PROGRESS.md`, `REVIEW.md`, `DECISIONS.md`, `HUMAN_TODO.md`, or any task file.

## Files you own (and nobody else touches this round)

`static/well-known/mcp/server-card.json`, `tests/test_mcp.py`, `src/registry_mcp/api/main.py` (**only** the
`include` `Query(description=…)` on the company route, ~line 798), `static/llms-full.txt`, `static/llms.txt`
(only if it already mentions `include` — it does not; leave it), `README.md`, `docs/clients.md`, `CHANGELOG.md`.

**Do not touch** `src/registry_mcp/core/`, `src/registry_mcp/mcp/`, `src/registry_mcp/registries/`, or any
other test file. Another agent (T40) owns `registries/` and `tests/test_attachments.py` this round.

## 1. The card's `inputSchema` is stale, and nothing catches it (finding 5)

`lookup_company.inputSchema.properties.include` in the card still carries the pre-`e03a518` description
("e.g. ['charges'] for United Kingdom (country='GB')…") and examples `[["charges"], []]`;
`src/registry_mcp/mcp/server.py` `_INCLUDE_DESCRIPTION` / `_INCLUDE_EXAMPLES` say otherwise. The drift test
`test_server_card_tools_and_prompts_match_the_live_server` compares names, descriptions, titles and prompts;
`test_server_card_lookup_company_output_schema_matches_model` compares one `outputSchema`. Neither looks at
`inputSchema`.

- Regenerate **every tool's `inputSchema`** in the card from the live server, the same way the drift test
  reaches it (`fastmcp.Client(mcp)` → `list_tools()` → `tool.inputSchema`). Do not hand-edit the JSON.
  Preserve the file's existing formatting (indent, key order of the top-level entries, trailing newline) —
  diff should show only the schema bodies that changed.
- Extend `test_server_card_tools_and_prompts_match_the_live_server` to assert
  `entry["inputSchema"] == tool.inputSchema` for every tool, with a message naming the tool.
- **Mutation check, report the result:** change one character of `_INCLUDE_DESCRIPTION` locally, run the
  test, confirm it goes red, revert. If it does not go red, the test is wrong.

## 2. REST's `include` description names one of three attachments (finding 6)

`api/main.py` ~798–807: the OpenAPI description of `include` says "e.g. ?include=charges for United Kingdom".
Rewrite it to say what `_INCLUDE_DESCRIPTION` says, in REST terms: three attachments today — `filings`
(every country: what the entity has filed and when; Companies House the whole history, Bolagsverket filed
annual reports, Regnskapsregisteret filed annual accounts, the block's `notes` says which), `charges`
(GB only), `insolvency` (GB only); `?include=filings&include=charges` repeats the parameter; empty by
default costs one upstream request; an undeclared value is `bad_request` (400) naming the country's set;
`GET /v1/countries` → `supported_includes`. Keep it a description string; change nothing else on the route.
Check `tests/test_api.py` for an OpenAPI snapshot that pins this string and update it if so.

## 3. The static discovery documents do not know two of the three includes exist (finding 6)

- `static/llms-full.txt`: the only `include` mention is `include=["charges"]` (~line 362). Add a section
  (find the natural place — near the `lookup_company` tool documentation or the charges paragraph) that
  documents `include` as a whole: the three names, which countries declare each, the one-line question each
  answers (filings → "do they file, and on time"; charges → "are assets already pledged"; insolvency →
  "are they being wound up"), the two-level nullability (absent = not asked or fetch failed + a `notes`
  sentence; present-and-empty = the register lists none), the per-country scope of `filings`, the
  members'-voluntary-liquidation caveat on `is_liquidation`, and one REST + one MCP example. Every claim
  must be true of the code at `e03a518` — read `core/models.py` `FilingHistory`, `InsolvencyBlock`,
  `ChargeBlock` and the `lookup_company` docstring in `mcp/server.py`; do not invent behaviour.
- `README.md`: one short block (≤ 12 lines) where the tools are introduced, showing `include=["filings"]`
  on MCP and `?include=filings` on REST, naming all three and which countries. Keep the README's voice.
- `docs/clients.md`: one sentence, only if that file describes tool arguments; otherwise leave it.

## 4. `CHANGELOG.md` `[Unreleased]` is empty across every commit since `v0.3.0` (finding 7)

Write it from the commits, not from memory: `git log v0.3.0..HEAD --format='%h %s'` and read each one's
diff stat. Keep a Changelog headings. At minimum:

- **Added** — `SourceRef` and `include=[…]` on `lookup_company` / `GET /v1/{country}/company/{id}`
  (`Registry.lookup_with`, D-026(c), D-042); `CountryInfo.supported_includes`; `include=["charges"]` for GB
  (D-042); `include=["filings"]` for GB, SE, NO and `include=["insolvency"]` for GB (D-044); the
  `counterparty_check` and `register_coverage` prompts (`cf5f13f`); `CompanyReport.charges`, `.filings`,
  `.insolvency` (all `null` unless asked). Cite the decision per line.
- **Changed** — GB reports **no longer carry `registers["charges"]`** (`beb287f`): Companies House's
  `has_charges` flag is wrong (Tesco, nine charges, flag `false`), so the key is omitted rather than
  asserted; `include=["charges"]` is the answer. Say it plainly — it is a removed key on the wire. Also the
  Smithery listing rename if a commit records it; the `register_coverage` wording fix.
- **Fixed** — the dashboard chart on narrow screens (`13a5e4a`), and anything else `git log` shows.

Do not bump the version. Do not add a release date. Leave `## [Unreleased]` as the heading.

## Done-check (the orchestrator verifies)

- Suite green; counts reported; `mypy .` and `ruff check .` clean.
- `git diff --stat` shows only the files listed under "Files you own".
- The extended card test goes red under the one-character mutation and green after revert (you report both).
- `grep -c '"filings"' static/well-known/mcp/server-card.json` increased (the `include` examples now carry it).
- `grep -c "filings" static/llms-full.txt README.md` — both > 0; `grep -n "insolvency" static/llms-full.txt`
  shows the `include` documentation.
- Every `[Unreleased]` line maps to a commit hash you can name in your report.
