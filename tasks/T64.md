# T64 — "Let's gather calls": four free channels an agent can work, one attribution change, and Kim's list

Kim, 2026-09-10 ~10:30 local: *"let's gather calls."* Context: 578 calls since launch, all ours; the products round
(`~/mcp-growth/products/00-SYNTHESIS.md`) found no product to build for money and left one job — get real callers in
front of the free tool before the 2026-10-19 gate, spending nothing. The gate now counts distinct non-bot user agents
that made a real ask, real-ask calls per day, unsolicited contact, and anyone at the 60/min ceiling or asking for a key
(`~/mcp-growth/DECISION-GATE.md`, amendments A1–A8).

Standing rules for every part: **agents may submit to directories, open PRs and comment on GitHub; agents do not post
to Reddit, Hacker News, LinkedIn, X, Discord, forums, or send e-mail** — those are Kim's, and drafts go to `content/`.
No live lookups against `api.foretak.dev` (static routes fine), Companies House or Bolagsverket; never look up
`833286602` or `833285602`; no keys written; `uv sync --locked --all-extras`, the usual gate for any code, never
`ruff format`; commit on your branch at least every 30 minutes; do not merge or push our repo; do not edit
`PROGRESS.md`, `REVIEW.md`, `DECISIONS.md`, `HUMAN_TODO.md`, `tasks/`. Read `SUBMISSIONS.md` (the house record of every
listing, and its format), `~/mcp-growth/DISCOVERY.md` and `FINDABILITY.md` (what is already listed and what each surface
delivered — "directory copy is hygiene; expect nothing from it"), and `content/README.md` before writing a line.

## Part A — the registries and catalogs not yet worked (Sonnet, worktree `t64a`, edits `SUBMISSIONS.md` only)

Survey, then submit where a form or a PR suffices: the **Docker MCP Catalog** (`docker/mcp-registry` — read its
CONTRIBUTING and `servers/*/server.yaml` format; our root `Dockerfile` is dual-mode, stdio when `PORT` is unset — that
is what they build; open the PR from a fork under `foretak` if `gh repo fork --org foretak` works, else the user); the
**VS Code MCP gallery** and **Cursor's directory** (find out whether each ingests the official registry — we are
`io.github.foretak/registry-mcp` there at 0.4.2 — or needs a submission; do what is needed); **LobeHub** (a listing
existed calling this "pre-release 0.1.0"; issue #19266 was filed — check its state and whether the listing now reads
0.4.2); **mcp.so** (issue chatmcp/mcpso#3927, no reply since 2026-09-04 — one polite nudge comment, then leave it);
any other catalog `DISCOVERY.md` mapped as "not listed" that takes a submission without a login we lack. For each:
what it needed, what you did, the URL, and what `DISCOVERY.md` says its reach is. One row per channel in
`SUBMISSIONS.md` in the house format; never claim a capability the served card does not carry.

## Part B — attribution: know which channel a call came from (Sonnet, worktree `t64b`)

The gate needs "calls by channel" and today the usage log has only the user agent. Add an optional `src` query
parameter on the REST routes and on `/mcp` (verify **locally** that a Streamable HTTP client connecting to
`http://127.0.0.1:PORT/mcp?src=test` still completes `initialize` and `tools/list` — FastMCP must ignore the query
string; if it does not, `src` goes on REST only and you say so), logged as a new nullable `source` column in the
`calls` table (`core/log.py`; migrate with `ALTER TABLE … ADD COLUMN` guarded by a `PRAGMA table_info` check, the way
the existing schema is created), surfaced in `/v1/stats` as `by_source` and on the dashboard as one small table.
`src` values are free text truncated to 32 characters, sanitised to `[a-z0-9_-]`, never logged as part of `query`, and
`loggable_query` (D-040) is untouched. Then put `?src=` on our own install lines: `src=readme` in `README.md`,
`src=llms` in `static/llms.txt` and `llms-full.txt`, `src=card` in the server card's install hints if it carries any,
`src=docs` in `docs/clients.md`, `src=plugin` in the plugin's `.mcp.json`/skill, `src=article` for `content/` —
**not** in `server.json` (the registry validates the endpoint) and not in the manifest submitted to directories
(Part A) unless the format allows a plain URL with a query string. Tests: the column exists after migration on an old
database, `by_source` counts, the sanitiser, and the `/mcp?src=` handshake. Footprint: `core/log.py`, `core/stats.py`,
`api/main.py` and `api/dashboard.py` (small), `mcp/server.py` (only if the `src` must be read there), tests, the docs
listed. CHANGELOG line under `[Unreleased]`.

## Part C — the article that carries a number nobody else has (Opus B, worktree `t64c`, `content/` only)

`evals/reports/2026-09-10-accuracy.md` has a fact worth publishing: with the tools a Sonnet 5 agent got two of three
answerable company questions right and sourced; without them, 3 of 45 answers were confidently wrong and 34 were
hedges. Write `content/08-how-wrong-without-a-register/` in the house style (`content/README.md`: one canonical
Markdown plus the dev.to version with front matter, ≤1,200 words, every number from the report with its caveat
sentence, the install line with `?src=devto` once Part B has landed — write it in now, it is harmless before), title
in the reader's question, no marketing voice, the honest limits stated (31 authored cases, mocked registers, one
model, three trials). Then **publish it to dev.to** with `content/publish_devto.py` and the key at
`~/secrets/registry-mcp/devto-api-key.txt` (agents have done this before — read the script; never print the key).
Also write, for Kim to post: a **Show HN** title + 4-line text (`content/08-…/show-hn.md`), a **LinkedIn** post
(`linkedin.md`, Norwegian and English), and a 3-line **r/LocalLLaMA or r/ClaudeAI** version — all in `content/`, none
posted by you. Record the dev.to URL in the article's README.

## Part D — where people are already asking (Sonnet, worktree `t64d`, edits `FEEDBACK.md` "Outbound" only)

Find the places where someone is asking, in public, for what this tool does: GitHub issues and discussions on MCP
server collections, ERP/accounting MCP servers, agent frameworks ("company lookup", "Companies House MCP", "brreg",
"Bolagsverket", "organisasjonsnummer", "KYB agent", "supplier onboarding agent"), and the earlier comment on
`nordio-ai/brreg-mcp-server#7` (check for a reply). Post **at most five** comments, each one a direct, specific
answer to what was asked, naming the tool once with its install line and never overstating it (the served card is the
truth; no "UBO", no "fraud check"); skip anything older than 90 days or already answered. Log every comment in
`FEEDBACK.md` under "Outbound" with the URL and date, so replies can be traced. If a thread is a better fit for Kim
(a person asking for a call, a vendor), draft the reply into `content/` instead and say so.

## Kim's list (agents draft, Kim posts)

The four Reddit drafts already in `content/` (r/ClaudeAI, r/mcp, UK developers, Sweden developers); the Show HN and
LinkedIn posts from Part C; the free Claude plugin directory submission (HUMAN_TODO §7.9 track 2, five minutes); the
Cline README-only install test then `content/cline-marketplace-issue.md`; the five e-mails that are pivot 4's
trigger (PowerOffice, Tripletex, a Fortnox-marketplace developer, a Visena/Quickr customer, a Swedish
researcher/journalist — the questions are in `~/mcp-growth/products/03`, `07`, `06`, `05`).

## Done-check (the orchestrator verifies)

- A: `SUBMISSIONS.md` has a row per channel touched, with URLs; every claim in a submitted description matches the
  served card. B: an old `calls.sqlite3` migrates; `/v1/stats` returns `by_source`; `/mcp?src=test` handshake test
  green; suite/mypy/ruff clean; install lines carry `?src=`. C: the article is live on dev.to with its URL recorded;
  every number matches `2026-09-10-accuracy.md`. D: ≤5 comments, each logged, none overstating.
