# T63 — The publishable accuracy number: agent arm ×3 and no-tools baseline ×3 over the eval cases, written up

Owner: a Sonnet, dispatched by the orchestrator 2026-09-10 on Kim's "gogo". Worktree `~/registry-mcp-wt/t63`, branch
`t63`, cut from `main`. Output is **reports only** — no code, no fixture, no prose edit.

Why: the products round's synthesis (`~/mcp-growth/products/00-SYNTHESIS.md` §4.1 S5, from `01-assets-inventory.md`
F2 #10 and `~/mcp-growth/DECISION-GATE.md` §8.2) wants one citable fact before the 2026-10-19 gate: *with these tools,
how often does a real model answer a company question correctly, and without them, how often does it answer
confidently and wrongly?* The harness already measures both arms (`evals/README.md`: `--agent`, and `--baseline`,
which asks the 15 baseline-marked cases with the tools withheld and classifies the answer as correct / honest hedge /
confidently wrong). One trial each exists from 2026-09-09 and 2026-09-05; three trials each is the harness's own
noise-smoothing rule, and no write-up exists.

Read first: `evals/README.md` in full (the modes, the scoring, the noise rule, the E19 hard gate, what a "harness gap"
is), `evals/cases.json`, the existing reports in `evals/reports/` (`2026-09-05-agent-run-2.md`, the four
`2026-09-09-*` files and the two `-x3` files — reuse their vocabulary), `DECISIONS.md` D-048(i).

## Ground rules

`uv sync --locked --all-extras`. The eval runs are the **only** live calls: the Anthropic API, key from
`~/secrets/registry-mcp/anthropic-api-key.txt` into the environment of the run command only — never printed, logged,
written or committed. No call to `api.foretak.dev`, Companies House, Bolagsverket, brreg, GLEIF or Peppol (the harness
mocks them; if a case needs `--live`, skip it as the harness does). Never look up `833286602` or `833285602`. Run
one eval process at a time. Commit on `t63` after each run; do not merge or push; do not edit the board files.
Budget: the two runs at three trials are roughly 200 model calls; if the first run's cost (from `usage` in the
report) projects past $15 for both, stop after the agent arm and say so.

## The runs

1. `uv run --group eval python evals/run.py --agent --trials 3 --out evals/reports/2026-09-10-agent-x3.md`
2. `uv run --group eval python evals/run.py --baseline --trials 3 --out evals/reports/2026-09-10-baseline-x3.md`
   (use the harness's actual flag names — check `--help`; the model is whatever the harness defaults to, name it).

## The write-up — `evals/reports/2026-09-10-accuracy.md`

One page, in this order:

1. **The number, stated so it can be quoted**: e.g. "With the tools, a Sonnet 5 agent answered N of 31 company
   questions correctly across three trials (x%); without them, the same model was confidently wrong on M of 15
   (y%), honestly hedged on H, and right from memory on R." Use the harness's own classification words; give the
   per-trial counts and the mean; state the model, the date, the commit.
2. **The failures, each in one line** — case id, what the model did, whether the harness calls it a model finding or a
   harness gap (mock coverage, a Unicode-hyphen substring miss as in `2026-09-09-agent-after-E17-x3.md`), and whether
   it reproduced across trials.
3. **The E19 hard gate**: pass rate over the three trials, and what the model did on the failing trials (it is the
   documented "answers from memory without calling the tool" case).
4. **What the number does and does not claim**: 31 authored cases, mocked registers, one model, three trials; not a
   benchmark of the registers' data; the caveat sentence a reader must carry.
5. **Cost of producing it** (calls, input/output tokens, dollars at the model's list price).

## Done-check (the orchestrator verifies)

- The two `-x3` reports and the write-up exist and are committed; the write-up's counts match the reports.
- `git diff --stat main..t63` is inside `evals/reports/` only.
- The key appears nowhere in the diff (`git diff main..t63 | grep -c sk-ant` → 0).
