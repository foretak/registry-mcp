# T12 — Worked-example content

Owner: Opus B (growth)
Depends on: T10
Unblocks: T14

## Goal
Four articles in `content/`, each with a dev.to version (≤600 words), a Reddit r/mcp short version (≤150 words), and a Norwegian version (for kode24). Every article uses the **real** tool output from a running local server — no invented JSON. Include the exact prompt the agent was given and the exact tool calls it made.

1. `content/01-vat-check/` — "Check if a Norwegian supplier is VAT-registered before paying an invoice" (uses `lookup_company`, reads the MVA field).
2. `content/02-deadlines/` — "Find every filing deadline for a Norwegian AS this quarter" (uses `company_deadlines` with `today`).
3. `content/03-enrich-spreadsheet/` — "Validate and enrich a list of org.nrs in a spreadsheet" (uses `validate_company_id` + `lookup_company` in a loop; include the small CSV).
4. `content/04-add-your-country/` — "Adding your country's registry to registry-mcp in an afternoon": walks through the `registries/xx/` stub, the `Registry` interface, and what a PR needs. Recruits contributors.

Each folder: `devto.md`, `reddit.md`, `no.md`, plus any CSV/JSON used. `content/README.md` with a posting schedule (one per 2 days) for `HUMAN_TODO.md`.

## Files you own
`content/`, the posting-schedule section of `HUMAN_TODO.md`.

## Files you must not touch
Everything else.

## Done-check
Twelve markdown files exist; each dev.to version is ≤600 words (`wc -w`); every JSON block was produced by the running server (paste the command that produced it in an HTML comment above the block). Set T12 to `review`.
