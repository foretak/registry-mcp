# T26 — Third country: Sweden (Bolagsverket, `registries/se/`)

Owner: Orchestrator (coordination); sub-tasks below have their own owners.
Started: 2026-09-05 evening, on Kim's instruction ("set up a team creating the Sweden folder").
Depends on: nothing in code. Depends on **Kim** for go-live: Bolagsverket client credentials (see §Human step).
Target: ≤ 2 weeks wall-clock (guide Step 12). The UK took ~3 h spec→release; Denmark (T16) is still waiting on Erhvervsstyrelsen (~2026-09-23), so Sweden is built first and Denmark follows the same recipe.

## Why Sweden now
Free since 3 Feb 2025 under the EU high-value-datasets regulation, no contract, no fee, self-service credentials by email + SMS, OAuth 2 client credentials, OpenAPI 3.0.3, a test environment with test companies. The research library ranks it fourth overall and first of the "next tier" (`research/02-registers-landscape.md`, `~/research/registry-mcp/02-registers-landscape/02-sweden-bolagsverket.md`). It completes the Nordic set the project already speaks for.

## Ground rules (D-001/D-008, T15 carry-overs, Phase 6 freeze)
- Everything Swedish lives in `src/registry_mcp/registries/se/`. `country = "SE"`, `registry = "bolagsverket"`. One import line in `registries/__init__.py` (alphabetical: gb, no, se, xx). **Zero edits to `core/`, `api/`, `mcp/`.** If the module needs one, that is the finding: stop, write it in your final report, and the orchestrator takes it to Kim. The one known candidate is R-2 in `CORE_ROADMAP_SPEC.md` (`euid` + `advertising_protected`, two nullable fields); it is **not** authorised by this task — REKLAMSPÄRR goes into `notes` for now (see T26a).
- Same contract as NO/GB: `CompanyReport`, `SearchResult`, `DeadlineReport`, `ValidationResult`, `format_id`, `aclose`, `rules_markdown`, `requires_api_key`/`api_key_env` (D-017). Unknown is `None`, never guessed (D-004/D-009/D-011). Every deadline rule ships **only with a primary-source citation** (D-016/D-023 policy); anything unsourced is `VERIFY` and not implemented.
- Research rules apply to every fact written down (`research/AGENT_PRIMER.md`, library `BRIEF.md`): URL + fetch date, confidence tag, no invented numbers, no invented JSON field names. If the real response shape cannot be obtained, say so and build to the documented field list with `VERIFY` markers — do not fabricate a schema and present it as observed.
- **Production is off limits.** Nothing in this task calls `https://api.foretak.dev` (it pollutes the usage stats). Functional checks run against a local server or Docker. Never look up `833286602` anywhere.
- No agent commits; the orchestrator commits with the required trailers. No agent edits `PROGRESS.md` (orchestrator-owned); report progress in your final message. Never leave a server running in the foreground.
- Bolagsverket's own web pages are behind bot protection: use Wayback (`web.archive.org`) captures, the developer portal if it allows anonymous reads, GitHub/npm/PyPI code that already talks to the API, and riksdagen.se / lagen.nu for statutes.

## Sub-tasks

### T26r — Recon (Opus R): the facts the spec cannot invent
Runs in parallel with T26a. Output: (1) `~/research/registry-mcp/02-registers-landscape/02b-sweden-api-shape-and-law.md` in library format (source, publisher, published, fetched, confidence, verbatim quotes), and (2) a short implementer-facing digest `tasks/T26-recon.md` in the repo with the same facts and their confidence tags, no prose padding.
Find, in priority order:
1. **The actual JSON shape** of the free API (`https://gw.api.bolagsverket.se/vardefulla-datamangder/v1`): the OpenAPI 3.0.3 document or real request/response samples. Sources to try: Bolagsverket's WSO2 developer portal (anonymous API listing?), GitHub code search for `gw.api.bolagsverket.se` / `vardefulla-datamangder` (`gh api search/code` is authenticated), npm and PyPI packages naming Bolagsverket, Wayback captures of the API page and its linked spec. Record exact endpoint paths, request body shape (the org.nr is sent how — path, query or POST body?), the response envelope, and the field names for every item in the field table in `02-sweden-bolagsverket.md` §5. If the spec is behind a login, say so explicitly and record what the field table alone gives.
2. **The kundanmälan (access request) form**: exact URL, the fields it asks for, what arrives by email vs SMS, test vs production credentials, token endpoint and scopes, and whether the test environment lists its test companies publicly.
3. **Organisationsnummer check digit** from a primary source (Skatteverket, SCB or Bolagsverket): confirm or refute Luhn/modulus-10 over the first nine digits; how the ten digits are written (`556016-0680`); the first-digit legal-form groups; the sole-trader case (personnummer as org.nr, `NAMNSKYDDSLÖPNUMMER`). Also the VAT-number rule (`SE` + 10 digits + `01`).
4. **Statutes for deadlines**, from riksdagen.se or lagen.nu: årsredovisningslagen (1995:1554) 8 kap. 3 § (filing with Bolagsverket — the exact wording and the month count), aktiebolagslagen (2005:551) 7 kap. 10 § (ordinary general meeting within six months), Bolagsverket's förseningsavgift scale (amounts + the page), and whether Bolagsverket applies any weekend/holiday roll-forward to filing deadlines (they publish this; find the sentence). If Skatteverket deadlines for an AB (inkomstdeklaration 2 by financial-year-end class; moms; arbetsgivardeklaration) can be sourced cleanly, record them with the caveat that the free dataset does not give financial-year end or VAT period — otherwise leave them out.
5. **Code lists**: `ORGANISATIONSFORM` (Bolagsverket) and `JURIDISK FORM` (SCB) with the mapping table between them, `AVREGISTRERINGSORSAK`, and the `PÅGÅENDE AVVECKLINGS- ELLER OMSTRUKTURERINGSFÖRFARANDE` codes — as far as they are public.
6. **Licence wording** (the exact Swedish sentences on reuse and attribution) and the **rate limit** sentence, re-fetched with URL + date.
Done-check: both files exist; every fact carries URL + fetch date + confidence; a "could not be verified" list at the end; no production calls.

### T26a — Architect (Opus A): `SWEDEN_SPEC.md` + decisions
- Write `SWEDEN_SPEC.md` mirroring `UK_SPEC.md`'s structure (upstream API incl. auth and rate limit; fixtures plan; registry class attributes; `CompanyReport` field mapping table; `notes` rules; address mapping; search mapping; rules — identifier validation, roll-forward, deadlines with derivation ladders; client behaviour; legal-form table; status derivation; cache; errors; logging; confidence; `rules_markdown`). Use the library file `02-sweden-bolagsverket.md` as the starting brief and **`tasks/T26-recon.md` (T26r, concurrent) as the source of record for the API shape, check digit, statutes and code lists**: read it before finalising §1 and §5; if it does not exist yet when you get there, mark those sections `VERIFY-pending-recon` and tell the orchestrator in your report — you will get a second pass.
- Decisions to take, appended to `DECISIONS.md` as **D-032+** (D-031 is the last): (a) identifier normalisation `556016-0680` / `5560160680` / `SE556016068001` → ten digits, and the check-digit rule once sourced; (b) `NAMNSKYDDSLÖPNUMMER`: one org.nr can carry several sole-trader businesses — how `lookup` behaves (first match with a `notes` sentence? error? list in `notes`?) — pick and justify; (c) legal form: `ORGANISATIONSFORM` (Bolagsverket) drives `legal_form_code`/`legal_form_local`, SCB `JURIDISK FORM` is the fallback and is recorded where? (d) status derivation from three orthogonal signals (`AVREGISTRERAD`+reason, `PÅGÅENDE AVVECKLING…` incl. konkurs with date → `bankruptcy_date`/`status_detail`, SCB `VERKSAM` → what exactly, and what `is_active` means for SE); (e) `REKLAMSPÄRR` → `notes` sentence now, no core edit; add a boxed **recommendation for Kim on R-2** (yes/no, cost, and whether Denmark + Sweden together justify it) — the orchestrator surfaces it; (f) two secrets in a one-slot contract: `requires_api_key = True`, `api_key_env` names one variable — decide the env var names (`BOLAGSVERKET_CLIENT_ID` / `BOLAGSVERKET_CLIENT_SECRET`, or one combined var) and how the hint names both; test vs production base URL selection; (g) licence string for `SE` — Bolagsverket names no licence, so quote the regime, do not invent a licence name; (h) the sole-trader personal-data note, mirroring the ENK note (T20).
- Deadlines: same policy as D-016/D-023 — implement only what a primary source supports; the free dataset has no financial-year end, so the calendar-year assumption returns with the same `applies_because` honesty as NO. State per rule: statute, month count, roll-forward (sourced), `mandatory`, `recurrence`, `authority`, `source_url`.
- Numbered test list (the implementer builds exactly that), confidence anchors per D-005, and a §"Fixtures" saying which fixtures come from real samples (if T26r found any) and which are `VERIFY` shapes to be replaced by live recordings when the credentials arrive.
- Done-check: `SWEDEN_SPEC.md` exists; decisions appended; `uv run pytest -q -m "not live"`, `mypy`, `ruff` still green (no code yet); your report lists every `VERIFY` left open and the R-2 recommendation in one paragraph.

### T26b — Implementer (Sonnet): `registries/se/`
- Starts when the orchestrator hands over `SWEDEN_SPEC.md` (+ `tasks/T26-recon.md`). Copy the `xx/` template; `__init__.py` (`BolagsverketRegistry`, registered; lazy imports like `no/`/`gb/`), `client.py` (httpx; OAuth 2 client-credentials token fetched from the token URL and cached in memory until expiry with a refresh margin; 60 req/min token bucket; 5 s timeout, one retry on 5xx/timeout; 401/403 without credentials → `RegistryError(upstream_error)` with the env-var hint, never a crash at import; 404 → `not_found`; 429 → `rate_limited`; 24 h SQLite cache per D-006), `mapping.py`, `rules.py`.
- Fixtures under `tests/fixtures/bv_*.json`: real samples if T26r found any, else the spec's documented shape with a `VERIFY` header comment field and a `tests/fixtures/README.md` section listing the exact `curl` commands (token + lookup) to record the real ones when credentials arrive. Tests use `respx` like `test_client_gb.py`; isolate the cache with the same fixtures as GB.
- Tests: every numbered spec test; mapping tests per fixture; deadlines with `today`; validate normalisation (`556016-0680` → `5560160680`, `SE556016068001` → ten digits, wrong check digit rejected, nine digits rejected); REST≡MCP parity for SE (copy the GB parity test); `/v1/countries` and `list_countries` show SE with `requires_api_key` true; `registry://rules/SE` non-empty; tests that hard-code the country list updated.
- Done-check: `uv run pytest -q -m "not live"` green, `uv run mypy` strict, `uv run ruff check`; the live test marked `@pytest.mark.live` and skipped without credentials; a local server (`PORT=8081 uv run uvicorn registry_mcp.api.main:app`, background, killed afterwards) answers `/v1/countries` with SE and `/v1/SE/company/5560160680` with the D-007 `upstream_error` + hint when no credentials are set.

### T26c — Docs that do not need live output (Opus B), after T26b
- `CHANGELOG.md` "Unreleased": Sweden module, credentials pending. `README.md`: one line in the countries section ("Sweden — built, awaiting Bolagsverket credentials"; no example output until it comes from a running server). `KEYWORDS.md` §SE (organisationsnummer, Bolagsverket, företagsinformation, kontrollera företag). `mcp/server.py` tool docstrings gain the Swedish aliases (docstring-only edit, coordinate after T26b). `docs/clients.md` untouched. `content/04-add-your-country/*` already names Sweden — no change.
- Deferred to **T26d (go-live)**, after Kim's credentials land on Railway (`railway variable set`): live fixture recording, README example from the running server, `static/llms.txt` / `llms-full.txt` / `server.json` keywords, version bump to 0.3.0 everywhere the 0.2.0 bump touched (`pyproject.toml`, `packages/*`, both `package.json`, `server.json` ×3, `__version__`), deploy, smoke `/v1/SE/company/…`, release (PyPI, npm via tag `v0.3.0`, Smithery re-scan, Glama re-check), kode24/Swedish article.

### T26e — Architect review (Opus A)
- Same checklist as T15e, scoped to SE, in `REVIEW.md` §T26e: did T26b need any `core/` edit; are the deadline rules sourced; is every unverified shape marked; is the no-credentials path correct on both surfaces; time taken vs target.

## Human step (Kim) — Bolagsverket credentials
Fill in Bolagsverket's **kundanmälan** for "API för värdefulla datamängder" with an email address and a phone number; client id + secret for **test and production** arrive by email and SMS. No contract, no fee. The form sits behind bot protection, so Claude in Chrome does it with Kim watching; the orchestrator writes the instruction block once T26r has the exact URL and fields (`HUMAN_TODO.md` §7.7). Then: `railway variable set BOLAGSVERKET_CLIENT_ID=… BOLAGSVERKET_CLIENT_SECRET=…` (names final after D-032(f)), and T26d begins.
