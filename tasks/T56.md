# T56 — List the hosted server on `punkpeye/awesome-remote-mcp-servers`, and record three things the mail said

Owner: a Sonnet, dispatched by the orchestrator 2026-09-09. Worktree `~/registry-mcp-wt/t56`, branch `t56` (for
the two repo files below); the PR itself goes to a fork of another repository. Read first: `SUBMISSIONS.md`
(the house record of every listing — match its format), `content/anthropic-connectors-submission.md` §1–§4 (the
canonical listing name and descriptions T50 wrote — reuse them, do not invent new copy), `README.md`'s first
screen, `~/mcp-growth/DISCOVERY.md`'s 2026-09-08 correction (directory copy is hygiene; make it accurate, expect
nothing from it).

## Ground rules

- Agents may submit to directories and list repositories. **Agents do not post to Discord, forums, social media
  or e-mail.** The awesome-list bot asks for a Discord username — ignore it; leave it for Kim.
- `gh` is logged in as `fargerod-dotcom`. Fork to the `foretak` org if `gh repo fork punkpeye/awesome-remote-mcp-servers --org foretak` works, else to the user. Branch `add-company-check`. One PR. Never call `api.foretak.dev` beyond the unlogged static routes to confirm a URL resolves. Never write a key anywhere. Never look up `833286602` or `833285602`.
- In this repo you edit **only** `SUBMISSIONS.md` and `HUMAN_TODO.md`. Commit on `t56`; do not merge or push our repo.

## 1. The PR

Read the target repository's README and CONTRIBUTING (or whatever format its entries follow) **before writing a line**; match its category, its line format, its badge requirements (the local list required a Glama score badge — check whether this one does; ours is `[![foretak/registry-mcp MCP server](https://glama.ai/mcp/servers/foretak/registry-mcp/badges/score.svg)](https://glama.ai/mcp/servers/foretak/registry-mcp)`), and its rules on hosted-endpoint URLs and auth. The entry: the listing name from T50 ("Company Check — UK, Sweden & Norway business registers" unless the list's format forbids a long name), the hosted endpoint `https://api.foretak.dev/mcp` (Streamable HTTP, no auth), one sentence in task language naming the three registers and the attachments that are **live on production today** — read `PROGRESS.md`'s "Deploy" rows to know which: at the time of writing, `filings` (GB/NO/SE), `charges` and `insolvency` (GB); do not list what is merged but undeployed. Link the repo and the homepage. Open the PR with a short body, the same voice as PR #13631's (find it: `gh pr view 13631 --repo punkpeye/awesome-mcp-servers`). Record the PR URL.

## 2. `SUBMISSIONS.md`

- awesome-mcp-servers: #13631 merged 2026-09-07 (the local/stdio listing stands); #13893 (Sweden edit) closed 2026-09-08 by the maintainer — hosted servers moved to `awesome-remote-mcp-servers`; the new PR's URL and status.
- kode24: the editor declined 2026-09-08 ("more like documentation for a tool than a reader's post"). Status: declined; a rewrite as an opinion piece is Kim's decision.
- Glama connectors: the awesome-list bot points hosted endpoints at `https://glama.ai/mcp/connectors` — add as a pending row (Kim's admin page; the existing Glama listing is still at 0.2.0 per the board).

## 3. `HUMAN_TODO.md`

One short block, §7.11: (a) the awesome-mcp-servers bot's Discord question — Kim's choice, agents never answer; (b) kode24 declined — pitch again only with a rewritten reader's piece; (c) Glama connectors listing + refresh the stale 0.2.0 listing (browser).

## Done-check

- The PR exists, follows the target list's format, names only live capabilities, and its URL is in `SUBMISSIONS.md`.
- `git diff --stat` in our repo shows only `SUBMISSIONS.md` and `HUMAN_TODO.md`.
