# T58 — Opus review of the depth track before the 0.4.0 deploy: `e03a518..HEAD` (rounds 1–3 and the T46 sync)

Owner: Opus reviewer, dispatched by the orchestrator once T46 has merged. Worktree `~/registry-mcp-wt/t58`, branch
`t58`. Writes **`REVIEW.md`** (one new section, house style: checklist table, numbered findings with severity,
"what is factually untrue", "must land before the deploy", "what is worth keeping") and nothing else in the repo.

Scope: every commit since `e03a518` (the D-044 wiring) — `git log --oneline e03a518..HEAD` — read against
`DECISIONS.md` D-044 through D-047 and the task briefs `tasks/T39.md`–`T56.md`. The previous two reviews to match:
`REVIEW.md` § "S-series" (mutation-tested claims) and § "D-044 wiring" (the orchestrator's). Execute against a
pristine export of `HEAD` (`git archive HEAD | tar -x -C <scratch>`), not the working tree.

## Ground rules

- No live calls to `api.foretak.dev` except the unlogged static routes (`/health`, the served card, `/legal/*`,
  `/llms.txt`) to compare the *deployed* state with `HEAD`. Keyless GLEIF / SML / SMP / Directory / brreg reads
  are allowed to re-verify a fixture; Companies House and Bolagsverket are not called. Never look up `833286602`
  or `833285602`. Never write a key anywhere.
- Mutation-test the claims that matter, the way the S-series did: revert a guard, flip a constant, re-run the
  test that is supposed to catch it, restore. Report which went red and which did not.
- Commit `REVIEW.md` on `t58`; do not merge or push. Do not edit `PROGRESS.md`, `DECISIONS.md`, `tasks/`.

## What to test, at minimum

1. **The seven attachments end to end on a local server** (`uv run uvicorn …` on a free port, or the MCP client
   in-process): `filings` ×3, `charges`, `insolvency`, `financials` (NO), `lei` (GB/NO), `parents` (GB/NO),
   `peppol` (NO) — each present-and-empty state, each `bad_request` for a non-declaring country naming the
   right set, each failed-fetch state leaving the block absent with a `notes` sentence and a successful lookup.
   REST ≡ MCP for every one.
2. **The invariants the rulings name**: D-043(h) one upstream request for `filings`+`financials` on NO (count
   it); D-045(e) `lei` absent for SE and `effective_includes` everywhere; D-047(a) `lei`+`parents` one GLEIF
   search; D-046 the SMP host from the NAPTR (mutate the resolver to return another host — does the test catch a
   hardcoded one?), `false` only on NXDOMAIN/404, `null` never cached; D-045(a) the three charge flags never
   `False`; D-044(b) the scope note first on every `filings` block in all three countries.
3. **The person-bearing line**: grep the tree for anything that reads `practitioners`, `contact`,
   `ix:nonNumeric`, `description_values` beyond `made_up_date`, officer/PSC keys, and GLEIF `SOLE_PROPRIETOR`
   handling; confirm each relay is a category word or a company fact and nothing else (D-028, D-042(e),
   D-046(f), D-047(a)).
4. **The prose is true**: `_INCLUDE_DESCRIPTION`, the `lookup_company`/`company_deadlines` docstrings,
   `instructions`, README, `llms-full.txt`, the card — every claimed capability exists and every count/date
   quoted is in the code or a decision. The T31a sentence is gone everywhere (grep). The privacy policy and terms
   name three countries and carry no "Draft".
5. **The deploy delta**: served card vs `HEAD` card; `/health` version vs `pyproject`; the CHANGELOG
   `[Unreleased]` covers every wire-visible change since `v0.3.0` (list the commits it misses); every manifest at
   0.4.0; `dnspython`'s licence recorded somewhere a distributor can find it.
6. **The orchestrator's own commits** (merge resolutions, card regenerations, the `scripts/regen_server_card.py`
   addition, the reconciled test in `c854e9e`) — the standing rule says the orchestrator does not write
   production code; say whether any of these crossed that line and whether they are correct.

## Verdict

APPROVED / APPROVED WITH FIXES / CHANGES REQUIRED, with the fixes that must land **before** `railway up` listed
first and each one sized. The orchestrator deploys only on APPROVED or after the "before" list is applied.
