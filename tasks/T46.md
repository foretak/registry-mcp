# T46 — Round-closing surfaces sync: every new block reaches the words an agent reads, the card, the docs, the changelog, and the 0.4.0 manifests

Owner: a Sonnet, dispatched by the orchestrator **after T38, T42, T45, T44 and T47 have merged** (rounds 2 and 3).
Worktree `~/registry-mcp-wt/t46`, branch `t46`. This is the one task in the round allowed to edit the tool prose and
the server card; every implementer before you was told to leave them to you.

Read first: `DECISIONS.md` D-042(d) (discoverability is paid in description text — the block names and the words
*financial figures, turnover, profit, balance sheet, solvency* (D-043(b)) and *LEI, legal entity identifier, GLEIF*
(D-045(e)) must reach `lookup_company`'s description, not only the schema enum), D-043(b),(i), D-045(a),(e),(g);
`tasks/T39.md` (the previous sync — same job, same method; its CHANGELOG mapping is the format to continue);
`tasks/T31.md` Part A (T31a's remaining lines); `REVIEW.md` § "D-044 wiring" findings 5–7.

## Ground rules

- `uv sync --locked --all-extras`. `uv run pytest -m "not live" -o addopts=""` (never `-q`), `uv run mypy .`,
  `uv run ruff check .`. Record the baseline at your branch point; report counts after.
- **Never run `ruff format` unscoped.** No live calls to `api.foretak.dev`, Companies House, Bolagsverket, brreg or
  GLEIF — every example in the docs is regenerated from a **local** server (`uv run uvicorn …` on a free port, or
  the MCP client in-process), the way T15c and T26d did, never from production.
- Never look up `833286602` or `833285602`. Never write a key anywhere. Commit on `t46`; do not merge or push.
  Do not edit `PROGRESS.md`, `REVIEW.md`, `DECISIONS.md`, `HUMAN_TODO.md`, `tasks/`.

## Files you own

`src/registry_mcp/mcp/server.py` (**only** `_INCLUDE_DESCRIPTION`, `_INCLUDE_EXAMPLES`, the `lookup_company` and
`company_deadlines` docstrings, and `instructions` if a block name belongs there), `src/registry_mcp/api/main.py`
(**only** the `include` query descriptions on the company and deadlines routes and `_DESCRIPTION`),
`static/well-known/mcp/server-card.json`, `static/llms-full.txt`, `static/llms.txt`, `README.md`, `docs/clients.md`,
`CHANGELOG.md`, `mcpb/manifest.json`, `server.json`, `packages/*/package.json`, `pyproject.toml`,
`.claude-plugin/**`, `plugins/registry-mcp/.claude-plugin/plugin.json` (T50 left it at 0.3.0 for this sweep) and any other manifest that carries the version string (find them: `grep -rln "0\.3\.0" --include=*.json --include=*.toml --include=*.md . | grep -v CHANGELOG`), `tests/test_mcp.py` (card tests only).

**Do not touch** `core/`, `registries/`, any test other than `test_mcp.py`, `legal/`.

## 1. Prose: the block names reach the retrieval key (D-042(d)(2))

- `_INCLUDE_DESCRIPTION` and `lookup_company`'s docstring gain `financials` (Norway only: the register's own key
  figures — revenue, operating result, profit, total assets, equity, liabilities — for the latest filed accounting
  period, every figure beside its currency, nothing derived; answers *is this supplier solvent*; a `None` figure
  inside a present block means the company did not report that line, an absent block means the country does not
  publish figures) and `lei` (every country except Sweden — say why in one clause: the identifier would leave for a
  third-party host, and a Swedish identifier may be a person's; GLEIF, CC0, keyless; present block with `lei: null`
  means GLEIF holds no LEI). Read `core/models.py`'s `FinancialSummary`, `FinancialPeriod` and `LeiRecord`
  descriptions and the D-043/D-045 text before writing; every claim must be true of the merged code.
- **`peppol` (Norway only, T43 / D-046):** `_INCLUDE_DESCRIPTION` and the `lookup_company` docstring must carry the words *Peppol*, *ELMA*, *e-invoice*, *EHF* and *2027* — whether the entity can receive an electronic invoice over the Peppol network, read live from the SML/SMP walk; `registered: null` means we could not ask, never "no"; `can_receive_invoice` is `True` or `None` from the Directory and `False` only from the authoritative route. Read `core/models.py`'s `PeppolParticipant` and D-046 before writing; `company_deadlines` gains **nothing** for it (D-029(f), and `DEADLINE_INCLUDES` must not contain `peppol`).
- `_INCLUDE_EXAMPLES` gains `["financials"]`, `["lei"]` and `["peppol"]` combinations that are valid for the example country
  (`_COUNTRY_EXAMPLES[0]` is `NO`, so a Norway-valid combination first).
- `company_deadlines`' docstring (both surfaces) says `include=["filings"]` is accepted and what it changes
  (T44 landed it — read `DEADLINE_INCLUDES`); the false sentence T31a names is gone from both surfaces (T44 removed
  it from the code; you check the surfaces' remaining prose).
- `instructions`: one clause naming the six attachments by name, nothing more.

## 2. The card

`uv run python scripts/regen_server_card.py` does this now (added 2026-09-09; read it first) — run it after every prose edit. Regenerate **every tool's** `inputSchema` and `outputSchema`, and every prompt, from the live in-process server
(`fastmcp.Client(mcp)` → `list_tools()`/`list_prompts()`; `tool.input_schema`, `dereference_refs(Model.model_json_
schema())` per `tests/test_mcp.py`), preserving the file's formatting. The drift tests must pass unchanged; if one
fails on a field you did not touch, that is a real drift from rounds 2–3 — fix the card, not the test, and report it.

## 3. Docs

- `static/llms-full.txt`: extend the `include` section T39 wrote with `financials` and `lei` (scope, empty states,
  the currency rule, the Sweden exclusion for `lei`), regenerate the examples from a local server, and delete
  T31a's two remaining "does not publish the financial year" sentences, replacing each with D-041(a)'s corrected
  statement (`include=["filings"]` carries the published year end; the calendar-year assumption is the fallback).
- `README.md`: the attachments block lists all six names with countries; T31a's line corrected the same way;
  the "Seven tools, not fifty" sentence still true (D-042(c): no tool was added — verify with `tools/list`).
- `static/llms.txt` and `docs/clients.md`: one line each only if they list capabilities.

## 3b. Three loose ends from round 2, all small

- `NORBIZ_SPEC.md` §5.5 still marks "Regnskapsregisteret key figures" DEFERRED; T38 shipped them. One paragraph, true of the code.
- `core/gleif.py` keeps a shared `httpx.AsyncClient` with an `aclose()` that nothing calls at shutdown, because `api/main.py`'s shutdown loop closes registered *countries* and GLEIF is not one. Add one call to `gleif.aclose()` in that lifespan shutdown (your `api/main.py` ownership is widened to that one line) and, if T43 landed a Peppol client with the same shape, the same for it. A test in `tests/test_api.py` that the lifespan closes it is welcome; do not restructure the loop.
- `core/models.py`'s `FinancialSummary` docstring says "Norway fills this block today; Sweden and Britain do not" — stale once T55 lands (Sweden fills it from the filed K2 report; Britain does not, and the reason is now the register's own — D-047(f)). Your `core/models.py` ownership is widened to that one docstring; regenerate the card afterwards.
- The card's `lookup_company.outputSchema` was regenerated by the orchestrator at each round-2 merge to keep `main` green, but `inputSchema` (the `include` enum/examples/description) and every prompt were not — §2 still applies in full.

## 3c. The privacy policy and terms are wrong on the wire (T50 found it; blocking for the Connectors Directory)

`legal/privacy.md` as served at `/legal/privacy` says the service covers "Norway … and the United Kingdom", contradicts itself about Sweden three paragraphs later, and opens *"Draft written 2026-09-05 … effective once published at a public URL"* on a page that is published. `content/anthropic-connectors-submission.md` §12 OPEN-1 specifies the four exact edits — make them, no more: three countries named consistently, the draft preamble replaced by an effective date (the date of your commit), nothing else rewritten. `legal/terms.md` has the same three-country gap (OPEN-2) — fix that sentence only; T47 already changed the `parties_entitled` sentence, do not touch it. `legal/` is added to your footprint for these edits only. A test in `tests/test_api.py` that `/legal/privacy` no longer contains the word "Draft" and names all three countries is welcome.

## 4. CHANGELOG and version

- `[Unreleased]` gains, from the commits since T39's entry (`git log 4693ac9..HEAD --format='%h %s'`): **Added**
  `include=["financials"]` (NO, D-043), `include=["lei"]` (GB, NO; SE `bad_request` by D-045(e)), the per-kind cache
  TTL table (D-045(e)), `include=["filings"]` on `company_deadlines` and Sweden's deadline rung 2 (D-041, D-045(g));
  **Changed** the charge block per D-045(a) (T47 wrote this line — check it is there and accurate), the `Charge`
  free-text `notes` sentence; **Fixed** the Swedish "does not publish the financial year" sentence (T31a), the
  usage log counting an internal error as a success (T45), the missing-method guard (T42). Each line cites its
  commit and decision. Leave the heading as `[Unreleased]` — the orchestrator dates it at tag time.
- **Version 0.3.0 → 0.4.0** in every manifest the grep above finds (T34's list is the precedent: `pyproject.toml`,
  `packages/*/package.json` ×2, `server.json`, `mcpb/manifest.json`, the plugin marketplace entry, `src/registry_mcp/
  __init__.py` or wherever `__version__` lives, the `uv.lock` project entry if present). `/health` must report
  0.4.0 from a local run. Do not tag.

## Done-check (the orchestrator verifies)

- `tools/list` from a local server: still 7 tools (5 + 2 aliases); `lookup_company.description` contains
  *financial figures*, *solvency*, *LEI*, *GLEIF*, *Peppol*, *e-invoice*, *filings*, *charges*, *insolvency*.
- Both card drift tests pass; a one-character mutation of `_INCLUDE_DESCRIPTION` turns the `inputSchema` test red.
- `grep -rn "does not publish the financial year\|does not publish a company's financial year\|which the free dataset does not publish" README.md static/ src/ content/ docs/` → empty (T44 retired it from `src/` and `SWEDEN_SPEC.md`; it survives at `static/llms-full.txt:823`, `README.md:150`, `content/reddit-sweden-developers-post.md:37` — the last is a draft Kim posts, fix the sentence, keep the post).
- `grep -rn "0\.3\.0" --include=*.json --include=*.toml --include=*.py . | grep -v CHANGELOG | grep -v uv.lock` → empty; local `/health` → `0.4.0`.
- Every example JSON in README/llms-full was regenerated locally (say from which local port and which commit).
- Suite green with counts; mypy, ruff clean; `git diff --stat` limited to owned files.
