# T51 — Architect: D-047 — corporate parents on the `lei` block, the FCA build brief behind Kim's key, UK filed documents as links, and the two registrations only Kim can make

Owner: Opus A (architect), dispatched by the orchestrator 2026-09-09 on Kim's "more depth". Worktree
`~/registry-mcp-wt/t51`, branch `t51`. Read first: `DECISIONS.md` D-045 (all parts — (c) ruled the FCA mechanism,
(e) the LEI, (h) the closed doors) and D-046; `tasks/T42.md` and `src/registry_mcp/core/gleif.py` as merged (the
LEI as built: `universal_includes`, `LeiRecord`, the 7-day TTL, the `registeredAs` query); `~/mcp-growth/DEPTH.md`
§3.2 (FCA, HMRC VAT), §6; `research/07-product-improvements.md` items 10 (the `parent_id` group-walk), 13 and 15;
D-028 (the bar on natural persons — every ruling below must stay on the company side of it); D-042(g),(j).

## Ground rules

- One `DECISIONS.md` entry, **D-047**, house style, append only. No `src/` edits. Briefs in the style of
  `tasks/T42.md`; implementers do not touch `mcp/server.py`, `api/main.py` or the card (a surfaces-sync task does).
- Keyless public reads (GLEIF, FCA's public docs pages, HMRC's developer hub pages) are allowed for verification
  and recorded. Never call `api.foretak.dev`, Companies House or Bolagsverket. Never look up `833286602` or
  `833285602`. Never write a key anywhere. Commit on `t51`; do not merge or push. Do not edit `PROGRESS.md`,
  `REVIEW.md`; `HUMAN_TODO.md` you may extend (part (d)).

## The parts

**(a) Corporate parents on the `lei` block.** GLEIF's Level 2 relationship data — `lei-records/{lei}/direct-parent`
and `/ultimate-parent`, plus the reporting exceptions when a parent is not disclosed — is CC0, keyless, and names
**legal entities only**. Rule whether `LeiRecord` gains `direct_parent` / `ultimate_parent` (each: the parent's LEI,
its legal name, its jurisdiction, and the relationship's status/period as GLEIF publishes them) and a
`parent_reporting_exception` (the reason GLEIF publishes when no parent is reported — *NON_CONSOLIDATING*,
*NATURAL_PERSONS*, *NO_KNOWN_PERSON* …). **The rule that decides it:** the `NATURAL_PERSONS` exception category
means the ultimate parent is one or more natural persons — a *category word*, not a name, and relaying the word is
company data; relaying anything beyond it is not. Say that. Count the cost: two more GLEIF requests per lookup
when asked — is that a second include value (`include=["lei"]` stays one request; a new `group`/`parents` value
pays for the two more) or a widening of `lei`? D-042(c)'s "a tool is bought by a distinct question" applies one
level down: *who is this company's parent* is a different question from *what is its LEI*. Argue it; rule the
cache kind and TTL (GLEIF relationship records change as slowly as LEIs); verify the endpoints and shapes with
one live resolution (Equinor's LEI, then a subsidiary with a disclosed parent — find one). Write **`tasks/T53.md`**
(Sonnet, core-only, same constraints as T42) if ruled in.

**(b) The FCA Financial Services Register, firm endpoints, behind `include_api_key_env`.** D-045(c) ruled the
mechanism and deferred the build on sequencing. Now write **`tasks/T54.md`** for a Sonnet, gated on Kim holding a
key (part (d)): the include name (neutral — `regulator`? `authorisation`? argue it against D-042(g) and against a
future filler such as Finanstilsynet's konsesjonsregister for Norway, which is open — check
`~/research/registry-mcp/` and DEPTH.md for what is known about it), the block shape (firm reference number,
status, permissions summary, appointed-representative relationships as *firm* records only, the date the
register last changed; **no individuals**, D-028), `SourceRef` licence (read FCA's terms), the 50 requests /
10 s limit, the two-credential expression on `CountryInfo` (D-045(c)), and the done-check.

**(c) UK filed documents as links (research item 15).** Companies House's filing-history items carry
`links.document_metadata`; the Document API serves PDF/iXBRL behind the same key. Rule whether `FiledDocument`
gains a `document_url` (the register's own metadata URL, needing the caller's own key — is a URL that needs a
credential we hold but the caller may not honest to relay?), whether an MCP `resource_link` content block is the
right vehicle, and whether this is depth or decoration given `document_id` is already carried. Likely small or
"no"; say which and why in one paragraph.

**(d) The registrations only Kim can make — `HUMAN_TODO.md` §7.10.** (1) FCA register API: self-serve key at
`register.fca.org.uk/Developer` — the steps, what to store (`~/secrets/registry-mcp/fca-api-key.txt`, never the
repo), and the env var name T54 will read. (2) HMRC "Check a UK VAT number" v2: developer-hub account,
application, OAuth client credentials, "around 2 weeks" — steps, what to store, and the D-045-style ruling on
what the block may carry (registered name and address of a *business* — for a sole trader that is a person's;
`id_may_be_personal` is `False` for GB, so rule how a GB sole-trader VAT registrant is handled) as a short part
(e) here so the eventual brief has an authority. (3) The lawyer-reviewed basis D-028 needs before officers/PSC —
the research's "highest-demand real gap" — is not a registration but it is Kim's step; one line pointing at
D-028's four preconditions and `research/…/10-personal-data-and-gdpr-in-registers.md`, so it is on the list.

## Deliverables

`DECISIONS.md` D-047 (a)–(e); `tasks/T53.md` if (a) is ruled in; `tasks/T54.md`; `HUMAN_TODO.md` §7.10. Report:
the rulings in one sentence each, what you verified live, the one thing each brief's implementer will get wrong.
