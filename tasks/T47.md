# T47 — Reconcile `Charge` / `ChargeBlock` with D-045(a): one field struck, four added, two type tokens, and the terms sentence that is wrong

Owner: a Sonnet, dispatched by the orchestrator (round 3, alongside T44 — disjoint files). Worktree
`~/registry-mcp-wt/t47`, branch `t47`. **Starts only after `tasks/T40.md` has merged** — T40 deletes
`gb/charges.py`'s stand-in models, so the canonical `Charge`/`ChargeBlock` in `core/models.py` is the only
place the shape lives when you begin. Read the file before sizing anything.

Ruling: `DECISIONS.md` **D-045(a)** — read every paragraph; it names each field, the reason, and the words
each description must carry. Supporting: D-042(e)(3) (the `persons_entitled` → `parties_entitled` precedent
this ruling reuses twice), D-042(g) (anti-bend), D-042(j) (truncation, derived flags by table), D-011,
D-028(1), D-040, D-044(b) (the disclosure-in-`notes` pattern).

**The window is settled, so do not re-derive it:** the hosted service *has* served the unreconciled block —
the orchestrator verified on 2026-09-08 19:35Z that `api.foretak.dev`'s served server card carries
`ChargeBlock` (three occurrences), from the 11:19 local deploy. No package release (PyPI, npm, MCP registry)
carries it. Treat this as a **same-day correction on a live block with no published consumer**: no
compatibility shim, no deprecation period, one `CHANGELOG.md` `[Unreleased]` **Changed** entry that says a
field was removed and four added, citing D-045(a).

## Ground rules

- `uv sync --locked --all-extras` first. `uv run pytest -m "not live" -o addopts=""` (never `-q`),
  `uv run mypy .`, `uv run ruff check .`. Record the baseline at your branch point; report counts after.
- **Never run `ruff format` unscoped.** No live calls to `api.foretak.dev`, Companies House, Bolagsverket or
  brreg — every number you need is in the four committed charge fixtures (`tests/fixtures/ch_*_charges.json`,
  110 items) and D-045(a) already counted them; re-count to confirm, do not fetch. Never look up `833286602`
  or `833285602`. Never write a key anywhere.
- Commit on `t47`; do not merge or push. Do not edit `PROGRESS.md`, `REVIEW.md`, `DECISIONS.md`,
  `HUMAN_TODO.md`, `tasks/`.

## Files you own

`src/registry_mcp/core/models.py` (**only** the `Charge` and `ChargeBlock` class bodies), `src/registry_mcp/
registries/gb/charges.py`, `legal/terms.md` (one sentence), `tests/test_client_gb.py` (the charge tests),
`CHANGELOG.md` (one `[Unreleased]` entry), `UK_SPEC.md` (only where it lists charge fields).

**Do not touch** any other class in `core/models.py`, `core/registry.py`, `core/cache.py`, `mcp/server.py`,
`api/main.py`, the server card (the closing surfaces-sync task regenerates it from your model change), or
`registries/no/`, `registries/se/`.

## The work, in D-045(a)'s order

1. **Struck: `outstanding_count`** from `ChargeBlock`, and from the mapper. Its description conceded the two-meanings problem; the ruling explains why a shared model cannot carry it.
2. **Added: `part_satisfied_count: int | None`** on `ChargeBlock` — the register's own whole-company figure, `None` when unpublished; description says it was 0 in every observed payload and is not derived.
3. **Added on `Charge`: `contains_fixed_charge: bool | None` and `contains_negative_pledge: bool | None`**, beside `contains_floating_charge`. Read D-045(a)'s paragraph on the three flags before writing a line of mapper: the register emits each key **only when `True`** (86 / 59 / 18 of 110). `None` means *the register did not mark this instrument*, never "unknown" and never `False`; **nothing may convert the absence into `False`**. Each description says so in those words and names Løsøreregisteret's and företagsinteckningar's different taxonomies as the reason another filler may leave all three `None` forever (the `days_from_fee_point` precedent).
4. **Descriptions corrected: `created_on`, `delivered_on`, `satisfied_on`.** `created_on` is the date the *charge instrument* was created, not a record timestamp; `delivered_on` is the register's receipt date; the interval is the Companies Act 2006 s.859A delivery window. Names unchanged. No derived `days_to_delivery` or `registered_late` (D-041(d)).
5. **Descriptions checked for the accepted fields**: `classification` says it is the register's prose, not a code, and is not `FiledDocument.type_code`; `charge_number` says it is a per-company sequence, not unique across companies and not a lookup key; `charge_id` says it is an opaque handle absent on 19 of 110 items (all pre-2013).
6. **Free text (S-series finding 7 as ruled):** `assets_charged` and `obligations_secured` stay relayed verbatim and uncapped. **Add `assets_charged_type: str | None` and `obligations_secured_type: str | None`**, the register's own `particulars.type` / `secured_details.type` tokens verbatim (`short-particulars` / `brief-description`; `amount-secured` / `obligations-secured`). The `obligations_secured` description must say the register's type token was `amount-secured` on 19 of 19 observed items, so the field's name is a category the token disambiguates.
7. **One unconditional `notes` sentence on every block that carries either text** (i.e. whenever any `Charge` in the page has a non-`None` `assets_charged` or `obligations_secured`): the register's own free prose, relayed verbatim and not parsed, and it may contain particulars of property, account details or a natural person's name. Put it in the mapper beside the existing truncation note.
8. **`legal/terms.md`:** the sentence naming `parties_entitled` as "the one place a natural person's name can reach a response" is wrong; it must name `parties_entitled`, `assets_charged` and `obligations_secured`. Change nothing else in that file.
9. **`gb/charges.py` prose:** if any "D-042(h)" citation survived T40, replace it with D-045(a). The module's two open questions (`outstanding_count` arithmetic, `part_satisfied_count`) are now answered — rewrite those paragraphs to say what D-045(a) ruled and delete the questions.
10. **`CHANGELOG.md`** `[Unreleased]` → **Changed**: one entry, the removed field named, the four added fields named, the two type siblings, the `notes` sentence, cited to D-045(a).

## Tests

- Every new field has a fixture-backed assertion (Tesco `00445790` carries a negative pledge on at least one
  item — confirm; NatWest `SC090312` for `total_count` 137 vs 100 returned; `00000006` and `OC303675` for the
  rest). A test proves an item whose `particulars` lacks a flag key maps to `None`, **not `False`**, for all
  three flags. A test proves `part_satisfied_count` is read from the wire, not computed. A test proves the
  free-text `notes` sentence appears when a page carries text and does not when none does. A test proves
  `obligations_secured_type` is relayed verbatim. `test_charges_no_natural_person_among_recorded_parties_
  entitled` stays as it is.
- Update any existing assertion that named `outstanding_count`; report each one.

## Done-check (the orchestrator verifies)

- `grep -rn "outstanding_count" src/ tests/ static/ legal/ UK_SPEC.md` → **empty**.
- `grep -n "part_satisfied_count\|contains_fixed_charge\|contains_negative_pledge\|assets_charged_type\|obligations_secured_type" src/registry_mcp/core/models.py` → five fields with descriptions.
- `grep -n "parties_entitled" legal/terms.md` → the sentence names all three fields.
- `grep -rn "D-042(h)" src/registry_mcp/registries/gb/` → empty.
- Suite green with counts; mypy, ruff clean; `git diff --stat` limited to owned files.
- A dry run of the drift test will go red on the card's `outputSchema` (you changed the model) — **expected**;
  report it, do not regenerate the card. The surfaces-sync task owns it.
